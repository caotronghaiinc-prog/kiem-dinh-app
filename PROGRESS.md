# INCERT AI OS — PROGRESS TRACKER
> Cập nhật file này sau mỗi buổi làm việc.
> Paste cùng với PROJECT_CONTEXT.md khi mở chat mới.

---

## TRẠNG THÁI TỔNG QUAN
**Phase hiện tại:** Phase 2 gần như hoàn tất (M3 + M4 xong trọn luồng) + đã bắt đầu kéo M2 (Hợp đồng & Tài chính, vốn dự kiến Phase 3) lên làm sớm theo yêu cầu của Hải. Onboard người dùng dữ liệu giả (PROMPT-17) vẫn đang treo, chưa làm.
**Tuần hiện tại:** Từ PROMPT-37 (xem lịch sử đầy đủ ở bảng PROMPT LIBRARY bên dưới), đã làm thêm: **Nồi hơi** (PROMPT-38/39, data model+form+export Word, mẫu `noi-hoi.docx`) và **Nồi gia nhiệt dầu** (PROMPT-41, tương tự Nồi hơi) — hoàn tất thêm 2 loại thiết bị KTAT. **Quản lý dụng cụ đo** (M4, PROMPT-43/44): danh mục `inspection_tools` + mượn/trả `inspection_tool_loans` (ràng buộc 1 lượt mượn đang mở/1 dụng cụ), trang `/tools`. **Giấy chứng nhận hiệu chuẩn** (PROMPT-45/46/47): bảng lịch sử hiệu chuẩn `inspection_tool_calibrations` (giữ MỌI lần hiệu chuẩn, không chỉ lần mới nhất), upload file chứng nhận, bulk-import 33 giấy chứng nhận thật (scan PDF, trích thủ công vì không có OCR tiếng Việt trong sandbox) khớp theo số chế tạo — phát hiện thêm 3 dụng cụ NDT/bê tông chưa có trong danh mục gốc, đã bổ sung (migration 0026). Sau đó xử lý 1 bug: `/tools/[id]` 404 do cache `.next` cũ (không phải lỗi code, `rm -rf .next` là đủ). **Khóa biên bản sau khi xuất + luồng Xin sửa/Duyệt** (PROMPT-49/50/51/52): cột `is_locked` (có sẵn từ PROMPT-03, giờ mới thực sự dùng) tự khóa qua RPC `lock_inspection_history` (phải dùng security-definer RPC vì RLS with-check chặn chính inspector tự set is_locked=true) sau khi xuất Word; PROMPT-50 phát hiện hệ thống **chưa hề có tính năng Sửa bản ghi kiểm định** nên phải xây từ đầu (2 luồng: có/không checklist template); PROMPT-51 thêm bảng `inspection_edit_requests` — Admin luôn sửa được, Inspector phải gửi yêu cầu kèm lý do, Admin duyệt (trigger tự mở khóa)/từ chối. **Audit Log** (PROMPT-53/54/55, migration 0028+0029): trigger tổng quát `log_audit_event()` (security definer) gắn vào `equipment`/`customers`/`inspection_history`, trang `/audit-log` chỉ Admin xem — **có 1 bug thật đã vá**: migration 0028 gốc so sánh `TG_OP` (luôn viết HOA) với chuỗi viết thường nên `old_data`/`new_data` luôn null, sửa ở migration 0029. **Module Hợp đồng — M2 v1** (PROMPT-56/57/58, migration 0030): kéo lên làm sớm vì Hải thấy phần mềm cần có hợp đồng mới đủ dùng thực tế trước khi mời đồng nghiệp dùng thử. 3 bảng `contracts` (mã tự sinh `HD-<năm>-NNN`, `contract_no` là số hợp đồng thật tự nhập)/`contract_equipment` (1 hợp đồng nhiều thiết bị)/`contract_payments` (nhiều đợt thu, tự cộng dồn `paid_total` qua trigger) — KHÔNG theo dõi báo giá riêng (vẫn dùng skill Cowork), audit log mở rộng sang 2 bảng tài chính này luôn. Trang `/contracts` + chi tiết (gắn/gỡ thiết bị, ghi nhận thanh toán, công nợ tự tính) + widget dashboard "Hợp đồng còn công nợ".
**✅ PROMPT-59 ĐÃ XONG (21/08/2026)** — đơn giá/số lượng/đơn vị tính/số tem/ngày kiểm định từng thiết bị trong hợp đồng + `total_value` tự tính qua trigger + nút "Xuất bảng kê thiết bị" (dùng thư viện `docx`, không cần mẫu). Migration `0031_contract_equipment_details.sql` đã Hải tự chạy tay trên Supabase SQL Editor, xác nhận xong. Claude Code đã tự smoke-test qua Playwright (viết→chạy→xóa, không commit rác), `tsc --noEmit` + `npm run build` sạch — xác nhận đủ: form tạo hợp đồng hết ô nhập tay giá trị; cột Mã hiệu đúng cho cả 2 kiểu key (`ma_hieu`/`loai_ma_hieu`) lẫn thiết bị không có mã hiệu (hiện "—"); dialog "Sửa" cập nhật đúng `total_value` + dòng "Tổng cộng" qua nhiều lượt sửa/gỡ thiết bị; RLS UPDATE cho inspector hoạt động đúng; file `.docx` xuất ra bóc tách XML khớp đúng nội dung. File mới: `edit-contract-equipment-dialog.tsx`, `export-equipment-list-button.tsx`, `contract-equipment-form-schema.ts`, `contract-equipment-list.ts`; sửa `types.ts`/`form-schema.ts`/`contract-form.tsx`/`contract-equipment-section.tsx`/`contracts/[id]/page.tsx`/`spec-fields.ts`; thêm dependency `docx`. **ĐÃ COMMIT + MERGE MASTER** (nhánh `claude/prompt-59-chi-tiet-thiet-bi`, commit [f87a7d4](https://github.com/caotronghaiinc-prog/kiem-dinh-app/commit/f87a7d4), 15 file — không dính `mau-bien-ban/*.docx` hay prompt untracked khác; merge `--no-ff` không conflict; push `5bda5b0..9fb652d master`; `tsc`/`build` sạch cả trước lẫn sau merge; `git status` sạch). Merge commit trên `master`: `9fb652d`.
**⚠️ Lưu ý dữ liệu thật**: DB production hiện có 1 hợp đồng (`HD-2026-003`, số `15/26/incert`) với `total_value` đang LỆCH (150.000.000đ, giá trị nhập tay cũ) vì dòng `contract_equipment` của nó chưa bị sửa/thêm/xóa lần nào sau migrate nên trigger chưa có dịp tính lại — đúng như đã chốt trước (không viết script backfill). Sẽ tự đúng khi ai đó mở dialog "Sửa" dòng thiết bị đó nhập lại số lượng/đơn giá thật. Không phải bug, không cần xử lý thêm.

**✅ PROMPT-60 ĐÃ XONG (21/08/2026)** — Module Báo giá mới (`quotes`/`quote_items`, mã tự sinh `BG-<năm>-NNN`) + liên kết "Tạo hợp đồng từ báo giá". Migration `0032_quotes.sql` đã Hải tự chạy tay trên Supabase SQL Editor, xác nhận xong. Claude Code tự smoke-test qua Playwright (viết→chạy→xóa, không commit rác) + `tsc --noEmit` + `npm run build` đều sạch: lưu đầy đủ báo giá cho cả khách đã có trong CRM lẫn khách tiềm năng (qua 5 trường snapshot, không bắt buộc `customer_id`); thêm/sửa/xóa hạng mục (`quote_items`) tính đúng `total_value` qua trigger; nút "Xuất báo giá" dựng `.docx` bằng thư viện `docx` (không cần mẫu), lưu vào Storage + ghi `quote_file_path`; nút "Tạo hợp đồng từ báo giá" bị khóa đúng khi báo giá chưa gán `customer_id` thật, mở đúng khi đã gán; luồng chuyển đổi copy đúng `quote_items` có `equipment_id` sang `contract_equipment`, hạng mục KHÔNG có `equipment_id` bị bỏ qua kèm toast báo rõ số lượng bị bỏ qua (không im lặng), `quotes.converted_contract_id` được ghi lại đúng; RLS test đủ (SELECT mọi role, INSERT/UPDATE admin+inspector, DELETE admin-only) khớp policy mirror `contracts`. Check DB production: 0 `quotes`, 0 `quote_items` — chưa có dữ liệu thật, đúng như dự kiến (tính năng mới). File mới: `src/app/(dashboard)/quotes/` (danh sách + toolbar + form + phân trang + link đính kèm, và `[id]/` chi tiết + items-section + item-dialog + export-button + create-contract-button), `src/lib/quotes/` (status/form-schema/item-form-schema), `src/lib/reports/quote-export.ts`, `prompts/PROMPT-60-module-bao-gia.md`. Sửa: `contract-form.tsx` (logic prefill `fromQuoteId` + copy `quote_items` → `contract_equipment`), `contracts/new/page.tsx` (đọc `?fromQuote=`), `layout.tsx` (thêm nav link "Báo giá"). **Khoảng trống đã ghi nhận, CHƯA làm (ngoài phạm vi PROMPT-60)**: nhãn tiếng Việt cho `quotes`/`quote_items` trên trang `/audit-log` (chỉ phần mở rộng trigger `log_audit_event()` nằm trong phạm vi) — Claude Code đề xuất làm prompt nhỏ riêng mirror PROMPT-57→58, CHƯA quyết định làm hay không. **ĐÃ COMMIT + MERGE MASTER** (nhánh `claude/prompt-60-module-bao-gia`, commit [c61c841](https://github.com/caotronghaiinc-prog/kiem-dinh-app/commit/c61c841), 25 file — đúng phạm vi; merge `--no-ff` không conflict; push `9fb652d..9a51f1e master`; `tsc`/`build` sạch cả trước lẫn sau merge; `git status` sạch). Merge commit trên `master`: `9a51f1e`. Module Báo giá (tạo/sửa/xuất `.docx`, liên kết Báo giá ⇄ Hợp đồng) đã lên `master`, sẵn sàng dùng.

**✅ PROMPT-61 ĐÃ XONG (23/08/2026)** — Biên bản nghiệm thu hợp đồng tự sinh từ dữ liệu app, port nguyên bố cục 16 mục từ skill `skil-bb-nt`. Trước khi chạy, mentor đã rà soát lại prompt so với code THẬT trên master (không chỉ bản thiết kế) và sửa 3 điểm: đổi số migration `0032`→`0033` (0032 đã bị PROMPT-60 dùng); phát hiện cột `contract_equipment.unit` (đơn vị tính) tưởng đã có từ PROMPT-59 nhưng thực tế CHƯA (migration 0031 thật không có cột này) — bổ sung ngay trong migration 0033; làm rõ văn bản này PHẢI dùng khổ A4/dxa/Times New Roman đúng mẫu gốc, KHÔNG mirror kiểu bảng đơn giản (% width, font mặc định) mà PROMPT-59/60 đang dùng. **1 sự cố quy trình trong lúc làm**: Claude Code quên dừng lại xin Hải chạy migration 0033 trước khi test (khác quy trình đúng đã làm ở PROMPT-59/60) — debug nhầm hướng một lúc (tưởng máy chậm, hóa ra cột DB chưa tồn tại), đã tự phát hiện + xin lỗi + sửa; Hải xác nhận đã chạy migration (có thể 2 lần, nhưng `alter table add column` không tạo cột trùng nên không hỏng gì — đã kiểm tra schema sạch). Đã test end-to-end (Playwright + render `.docx` ra ảnh xem lại bằng mắt, dọn sạch dữ liệu/file test) + `tsc --noEmit`/`npm run build` sạch: render đúng 2 nhánh "Đạt yêu cầu"/"Có vấn đề" + trường hợp `signed_date` null — khổ A4 đúng, bảng không tràn lề, cột thẳng hàng dòng "Tổng cộng" (đúng 2 lỗi thật đã từng xảy ra ở skill gốc, tránh được cả 2); hàm `numberToVietnameseWords()` mới tự test độc lập 30+ giá trị (số 0 giữa, tròn chục/trăm, "mốt"/"lăm"/"linh") đối chiếu bằng mắt, đúng chuẩn; nút xuất disabled đúng khi chưa có `acceptance_date`; dialog nghiệm thu 2 nhánh lưu đúng, chuyển "Đạt" tự null `acceptance_note`; nhập "Đơn vị tính" qua dialog sửa thiết bị lưu đúng + hiện đúng trong file; nút file biên bản đã ký hiện đúng nhãn "Xem biên bản đã ký" (không lẫn "Xem file hợp đồng"); RLS inspector cập nhật được cả nghiệm thu lẫn đơn vị tính, không bị chặn ngoài ý muốn. File mới: `acceptance-dialog.tsx`, `contract-acceptance-section.tsx`, `export-acceptance-button.tsx`, `acceptance-form-schema.ts`, `contract-acceptance.ts` (builder Word), `number-to-words-vi.ts`, `prompts/PROMPT-61-...md`; sửa `types.ts`/`contracts/[id]/page.tsx`/`edit-contract-equipment-dialog.tsx`/`attachment-link.tsx` (thêm prop `label`)/`contract-equipment-form-schema.ts`. Không thêm cột "Đơn vị tính" vào bảng kê thiết bị PROMPT-59 (mục tùy chọn) — giữ phạm vi gọn theo đúng cho phép của prompt. **CHƯA COMMIT/MERGE** — Hải sẽ báo khi sẵn sàng.

**📝 PROMPT-62 mới soạn, đã mở rộng phạm vi (23/08/2026, chưa chạy)** — Bắt đầu từ phản hồi của Hải: file xuất Báo giá (PROMPT-60) "quá đơn giản", gửi kèm mẫu thật `Mau_Bao_Gia_INCERT.pdf` đang dùng thực tế (logo, letterhead, căn cứ pháp lý, "địa điểm thực hiện", bảng 3 dòng tổng Cộng chưa VAT/Thuế VAT 8%/TỔNG CỘNG, khối ký tên). Qua nhiều vòng AskUserQuestion, phạm vi mở rộng từ "chỉ sửa file xuất báo giá" thành "đồng bộ quy ước VAT giữa Báo giá VÀ Hợp đồng": Hải yêu cầu "đơn giá giữ hợp đồng và báo giá như nhau" — tức không chỉ đổi `quote_items.unit_price` sang "CHƯA gồm VAT" mà `contract_equipment.unit_price` (PROMPT-59) cũng đổi theo, thay vì phương án ban đầu (nhân 1.08 lúc chuyển đổi báo giá→hợp đồng, giữ 2 quy ước khác nhau). Đã hỏi và Hải xác nhận: **chưa có hợp đồng thật nào nhập đơn giá/số lượng thật qua dialog "Sửa"** — mọi `contract_equipment.unit_price` thật vẫn là 0 mặc định, nên đổi quy ước AN TOÀN, không cần backfill. Việc đồng bộ này kéo theo phải sửa THÊM 2 file đã có sẵn (phát hiện qua rà soát, không phải Hải yêu cầu trực tiếp): `contract-equipment-list.ts` (PROMPT-59, ĐÃ merge master — thêm 3 dòng tách VAT như báo giá, nếu không "Tổng cộng" sẽ thiếu 8%) và `contract-acceptance.ts` (PROMPT-61, CHƯA commit — chỉ sửa cách HIỂN THỊ cột Đơn giá/Thành tiền từng dòng cho khớp VAT với dòng "Tổng cộng (đã bao gồm thuế GTGT 8%)" đã có sẵn, KHÔNG đổi cấu trúc 16 mục/dữ liệu DB). Cũng làm rõ: mẫu báo giá dùng pháp nhân **INCERT** (Số 12 Đầm Sen 20, phường Ngũ Hành Sơn, Đà Nẵng) — KHÁC pháp nhân **INCOSAF** đã hardcode cho Biên bản nghiệm thu — 2 pháp nhân/chi nhánh khác nhau Hải đang vận hành, không gộp/đổi lẫn nhau. **✅ PROMPT-62 ĐÃ XONG (23/08/2026)** — migration `0034_vat_convention_and_site_location.sql` đã Hải tự chạy tay trên Supabase, xác nhận xong. Claude Code lần này dừng lại xin migration đúng lúc (rút kinh nghiệm từ sự cố PROMPT-61) — không debug nhầm hướng. Test end-to-end (Playwright + render `.docx` ra ảnh so với `Mau_Bao_Gia_INCERT.pdf` thật, dọn sạch dữ liệu test) + `tsc --noEmit`/`npm run build` sạch: báo giá render khớp gần như tuyệt đối với mẫu thật (letterhead 2 cột màu `#13577E`, quốc hiệu, tiêu đề, "Địa điểm thực hiện" hiện/ẩn đúng, căn cứ TT41, bảng 3 dòng VAT, GHI CHÚ 5 mục, chữ ký, footer lặp trang) — riêng logo không hiện trong ảnh preview do chính công cụ xem thử chặn `blob:` qua CSP (đã xác nhận riêng byte ảnh nhúng trong `.docx` khớp 100% `public/logo.png`, mở Word thật sẽ đúng, không phải bug); số học đúng (báo giá 2 hạng mục ra đúng `Cộng chưa VAT`/`Thuế VAT`/`TỔNG CỘNG`, khớp `quote.total_value` 124.248.600đ); "Tạo hợp đồng từ báo giá" copy thẳng không nhân VAT; nhập đơn giá hợp đồng 50.000.000đ (chưa VAT) → `contracts.total_value` tự tính đúng 54.000.000đ (gồm VAT — đúng 50tr × 1.08); bảng kê thiết bị (PROMPT-59) 3 dòng VAT đúng công thức; Biên bản nghiệm thu (PROMPT-61) cột Đơn giá/Thành tiền hiện đúng giá đã gồm VAT (54.000.000đ), khớp dòng "Tổng cộng"; RLS inspector sửa được `site_location`; `HD-2026-003` không bị ảnh hưởng, vẫn 150.000.000đ như ghi chú cũ. **Ghi chú quan trọng khi commit**: mục B trong prompt (bỏ nhân VAT ở `contract-form.tsx`) hóa ra không cần sửa gì — code PROMPT-60 đã copy thẳng `unit_price` sẵn từ đầu, chưa từng có bước nhân nào để bỏ (bản PROMPT-62 trước đó từng yêu cầu THÊM bước nhân 1.08 — bản cuối đã bỏ yêu cầu đó trước khi chạy, nên không phát sinh việc phải revert). File mới: `company-info.ts`. Viết lại hoàn toàn: `quote-export.ts`. Sửa: `contract-equipment-list.ts`, `contract-acceptance.ts` (vẫn sửa thẳng vì PROMPT-61 chưa từng lên master, không cần revert), `quotes/types.ts`/`form-schema.ts`/`quote-form.tsx`/`[id]/page.tsx`/`[id]/edit/page.tsx`. **CHƯA COMMIT/MERGE** — sẽ gộp chung với PROMPT-61 khi commit (vì PROMPT-61 chưa từng lên `master`, code PROMPT-62 sửa trực tiếp lên file PROMPT-61 chưa commit) — Hải sẽ báo khi sẵn sàng.
**Cập nhật lần cuối:** 23/08/2026

---

## ✅ ĐÃ HOÀN THÀNH

### Thiết kế & Kiến trúc
- [x] Xác định scope dự án: KTAT + Thiết bị XD + NDT
- [x] Chốt 6 module: M1-CRM · M2-HĐ/TC · M3-KĐ/BB · M4-TBĐo · M5-Lịch · M6-BC
- [x] Chốt tech stack: Next.js 14 + Supabase + Vercel + Claude API
- [x] Chốt lộ trình 3 Phase (6 tháng)
- [x] Chốt database schema Phase 1
- [x] Tạo PRD Phase 1 (file Word)
- [x] Tạo PROJECT_CONTEXT.md
- [x] Tạo PROGRESS.md (file này)

---

## 🔄 ĐANG LÀM

- [ ] **[PROMPT-17]** Hướng dẫn sử dụng cho team + Onboard 7-9 đồng nghiệp dùng thử với **dữ liệu giả**, thu thập góp ý trước khi dọn dữ liệu giả và chuyển sang dữ liệu khách hàng thật chính thức (xem thêm mục Audit Log ở "📌 Ý TƯỞNG BACKLOG" — cần làm trước bước chuyển dữ liệu thật) — song song, chưa có cập nhật mới
- [ ] **[PROMPT-61 + 62]** Cả 2 code+test xong, CHƯA commit/merge — sẽ gộp chung 1 lần commit vì PROMPT-62 sửa trực tiếp lên file PROMPT-61 chưa từng lên master. Chờ Hải xác nhận sẵn sàng.

---

## ⏳ VIỆC TIẾP THEO — Phase 1

### Tuần 1: Setup nền móng — ✅ HOÀN THÀNH (08/2026)
- [x] Tạo tài khoản GitHub, Supabase, Vercel
- [x] **[PROMPT-01]** Khởi tạo project Next.js 14 + TypeScript + shadcn/ui + Tailwind (gộp luôn kết nối Supabase)
  - Repo: `caotronghaiinc-prog/kiem-dinh-app`
  - shadcn/ui viết tay thủ công (registry ui.shadcn.com bị chặn trong sandbox Claude Code — không ảnh hưởng chất lượng, chỉ cần lưu ý khi thêm component mới sau này)
- [x] Kết nối Supabase + tạo database schema Phase 1 (4 bảng: customers, equipment, inspection_history, profiles)
  - Supabase project: `incert-ai-os`, region Singapore (ap-southeast-1)
  - Project ID: `bgdnvgcyxmtbzluuiawv`
  - Dùng bộ key "Legacy anon/service_role" (không dùng hệ mới publishable/secret) để khớp code
- [x] Deploy lần đầu lên Vercel — THÀNH CÔNG
  - URL production: https://kiem-dinh-app.vercel.app
  - Team Vercel: INCERT (gói Hobby — cân nhắc nâng Pro khi vận hành chính thức)
  - Trang /test-connection xác nhận: "Thành công — Đã kết nối thành công tới Supabase project" (đã XÓA ở PROMPT-15 — chỉ là trang debug lúc dựng pipeline, lộ thông tin cho mọi role đã đăng nhập, không cần thiết cho bản dùng thử)
- [x] **[PROMPT-03]** Setup Supabase Auth + phân quyền 2 vai trò (Admin/Kiểm định viên)
  - Migration: `supabase/migrations/0002_auth_rls.sql`
  - Cột `is_locked` (boolean) đã thêm vào `inspection_history` — chỗ móc cho M3 Phase 2 (xuất phiếu chứng nhận tự động khóa)
  - Trigger tự tạo `profiles` khi có user mới trong `auth.users`
  - Trigger chặn user tự đổi role/active của chính mình
  - RLS đầy đủ cho 4 bảng theo ma trận: Admin toàn quyền, Inspector chỉ sửa được equipment + inspection_history (không xóa), không sửa được customers, không sửa được inspection_history đã `is_locked = true`
  - Middleware bảo vệ toàn bộ route, chưa đăng nhập → redirect /login
  - Trang /login, /unauthorized, nút Đăng xuất, hiển thị role trên dashboard
  - Đã test qua Preview URL: middleware chặn đúng, login/logout hoạt động, 2 role hiển thị đúng trên UI
  - Đã merge vào master
  - 2 tài khoản test: caotronghai.inc@gmail.com (admin), caotronghai.incosaf@gmail.com (inspector)

### Tuần 2: CRUD Khách hàng
- [x] **[PROMPT-04]** Màn hình danh sách khách hàng (tìm kiếm, lọc, phân trang)
- [x] **[PROMPT-05]** Form thêm/sửa khách hàng (validation đầy đủ)
- [x] **[PROMPT-06]** Trang chi tiết khách hàng (4 tab)

### Tuần 3: CRUD Thiết bị — ✅ HOÀN THÀNH
- [x] **[PROMPT-07]** Màn hình danh sách thiết bị (lọc theo loại, trạng thái hạn)
- [x] **[PROMPT-08]** Form thêm/sửa thiết bị (gắn với KH, màu trạng thái hạn)
- [x] **[PROMPT-09]** Trang chi tiết thiết bị + lịch sử KĐ

### Tuần 4: Dashboard — ✅ HOÀN THÀNH
- [x] **[PROMPT-10]** Dashboard 4 widget chính
- [x] ~~**[PROMPT-11]**~~ Widget cảnh báo hạn KĐ (đỏ/vàng/xanh) — **đã gộp vào Widget 1 của PROMPT-10** (đếm 3 màu + danh sách 5 thiết bị hạn gần nhất, tính qua `getExpiryStatus()`), không cần làm PROMPT-11 riêng nữa
- [x] **[PROMPT-12]** Nút soạn tin nhắn Zalo qua AI — dùng **OpenAI** (ngoại lệ riêng cho tính năng này, xem ghi chú bên dưới), không phải Claude API như tên gọi gốc trong kế hoạch

### Tuần 5-6: Hoàn thiện
- [x] **[PROMPT-13]** Tìm kiếm toàn hệ thống
- [x] Logo + màu thương hiệu chính thức (bổ sung, không phải PROMPT đánh số gốc) — xem ghi chú bên dưới
- [x] **[PROMPT-14]** Responsive mobile
- [x] **[PROMPT-15]** Kiểm tra phân quyền 4 vai trò + rà soát bảo mật OWASP

### Tuần 7-8: Deploy & Onboard
- [x] **[PROMPT-15b]** Nâng cấp Next.js 14.2.35 → 16.3.0, vá 5 CVE HIGH severity
- [x] **[PROMPT-16]** Rà soát cuối cùng trước khi coi Vercel deploy là production chính thức
- [x] **[PROMPT-16b]** Thiết lập backup database tự động qua GitHub Actions (cần bạn tự thêm secret `SUPABASE_DB_URL` 1 lần)
- [ ] **[PROMPT-17]** Hướng dẫn sử dụng cho team + Onboard 7-9 đồng nghiệp dùng thử (dữ liệu giả)
- [ ] Audit Log (PROMPT riêng, sau khi có góp ý từ đợt dùng thử — xem "📌 Ý TƯỞNG BACKLOG")
- [ ] Dọn dữ liệu giả, chuyển sang dữ liệu khách hàng thật chính thức

---

## ❌ VẤN ĐỀ / BLOCKERS

*Chưa có*

---

## 📝 GHI CHÚ QUAN TRỌNG

- AI chỉ soạn thảo văn bản — KĐV quyết định chuyên môn
- Tích hợp MISA + Cổng DVC Bộ NV để sau (Phase 3+)
- Thí nghiệm XD (19 chỉ tiêu) bổ sung sau Phase 3
- Hóa đơn điện tử: tích hợp VNPT/Viettel ở Phase 3
- Sandbox Claude Code bị chặn network ra ngoài (ui.shadcn.com, supabase.co...) — không test được kết nối thật trong sandbox, phải deploy lên Vercel mới xác nhận được. Đây là giới hạn môi trường, không phải lỗi code.
- GitHub App của Claude cần cài thủ công qua https://github.com/apps/claude (lệnh /install-github-app không khả dụng trong môi trường đang dùng)
- Auth + RLS (2 role admin/inspector) đã xong, đã test qua Preview URL và merge vào master
- Cột is_locked đã có sẵn trong inspection_history, chờ M3 (Phase 2) gắn vào nút "Xuất phiếu chứng nhận"
- RLS phần "chặn xóa" mới xác nhận qua đọc policy, chưa test trực quan bằng UI thật (vì UI CRUD chưa có) — sẽ verify lại khi làm PROMPT-04/05
- 2 tài khoản test: caotronghai.inc@gmail.com (admin), caotronghai.incosaf@gmail.com (inspector)
- **Phân quyền `customers` đã đổi** (migration `0005_customers_insert_inspector.sql`): Admin CRUD toàn quyền. Inspector: chỉ **Tạo mới** được, KHÔNG Sửa/Xóa (kể cả bản ghi tự tạo). Accountant/Office: chưa triển khai policy riêng, để sau. Chi tiết + câu SQL chạy tay ở README mục 11.
- **PROMPT-12 dùng OpenAI, không phải Claude API** — đây là NGOẠI LỆ chỉ áp dụng cho tính năng soạn tin nhắn Zalo (API key OpenAI mua tạm để test). Mọi tính năng AI khác của dự án (M3 soạn biên bản, Phase 2...) vẫn giữ nguyên kế hoạch gốc dùng Claude API. Code đã tách riêng lệnh gọi provider vào `src/lib/ai/draft-message.ts` để sau này đổi sang Claude chỉ cần sửa 1 file này.
- **⚠️ Cần làm thủ công**: biến `OPENAI_API_KEY` mới thêm khung (rỗng) vào `.env.local`/`.env.example` — phải tự lấy key thật tại platform.openai.com rồi điền vào `.env.local` để test local, VÀ thêm vào Vercel (Project Settings → Environment Variables) để chạy được trên production/preview. Khác với `E2E_TEST_LOGIN_SECRET` (biến đó KHÔNG được đặt trên Vercel).
- **Logo + màu thương hiệu**: `public/logo.png` (wordmark đầy đủ, dùng ở header + `/login`) và `src/app/icon.png` (crop vuông riêng phần icon ngôi sao + "IN" — dùng làm favicon qua quy ước file metadata của Next.js, wordmark ngang gốc quá dẹt để làm favicon dễ đọc). Màu thương hiệu #13577E (đậm) làm `--primary`, #36B4E7 (sáng) CHỈ dùng cho `--ring` + tint nhạt của `--accent` — không dùng #36B4E7 làm nền nút chữ trắng vì tương phản chỉ 2.38:1 (dưới chuẩn WCAG AA 4.5:1), trong khi #13577E đạt 7.8:1. Chỉ đổi 3 biến `--primary`/`--accent`/`--ring` trong `globals.css`, không đụng màu ngữ nghĩa (đỏ/vàng/xanh lá đã dùng cho trạng thái hạn kiểm định).
- **Chỉnh lại bố cục ô tìm kiếm toàn hệ thống** (phản hồi thẩm mỹ sau PROMPT-14): tách `<GlobalSearch>` ra hàng riêng, full-width, ngay dưới hàng nav chính — giống nhau trên mọi kích thước màn hình. Bỏ hẳn logic thu gọn icon/overlay riêng cho mobile đã làm ở PROMPT-13/14 (không còn cần thiết vì input luôn hiện sẵn). Không đổi logic tìm kiếm/hiển thị kết quả, chỉ đổi vị trí.
- **[PROMPT-15] Migration 0010 đã CHẠY XONG trên DB thật** — sửa lỗ hổng phát hiện ở Phần C: policy INSERT của bucket Storage `inspection-files` (tạo ở migration 0009) trước đó chỉ check `authenticated` thay vì đúng `admin`/`inspector`. Xác nhận qua đối chiếu trực tiếp `SELECT * FROM pg_policies` trên DB thật (không còn dựa vào lịch sử migration) do bạn tự chạy và dán kết quả — policy tên `inspection_files_insert_admin_inspector` đã có mặt, đúng điều kiện.
- **[PROMPT-15] Đối chiếu `pg_policies` DB thật phát hiện thêm 1 điểm lệch lịch sử (không phải lỗ hổng)**: policy SELECT của bảng `profiles` trên DB thật tên `profiles_select_all_authenticated` (`using (true)`, `to authenticated`) — KHÁC với migration `0004_profiles_select_authenticated.sql` đã commit (`using (auth.role() = 'authenticated')`, không có mệnh đề `to`). Ai đó đã sửa tay qua Supabase Dashboard ngoài luồng migration, không rõ khi nào. Đã verify thực nghiệm bằng anon key (chưa đăng nhập): cả SELECT lẫn UPDATE đều bị chặn đúng (0 dòng) — hành vi vẫn an toàn, chỉ là repo không phản ánh đúng thực tế DB. **Migration `0011_reconcile_profiles_select_policy.sql`** đã viết để đồng bộ lại lịch sử (no-op về hành vi, chỉ ghi lại đúng policy đang chạy thật) — **cần chạy tay** trên Supabase SQL Editor như migration 0010, dù là no-op vẫn nên chạy để đánh dấu đã áp dụng, tránh nhầm lẫn khi xem lại lịch sử sau này.
- **[PROMPT-15] Next.js 14.2.35 có 5 lỗ hổng HIGH severity** (`npm audit`: DoS, SSRF, cache poisoning...) — đã hoãn sang PROMPT-15b, **xem PROMPT-15b bên dưới: đã nâng cấp xong, 5 CVE đã vá**.
- **[PROMPT-15b] Nâng cấp Next.js 14.2.35 → 16.3.0, React 18 → 19.2.8**: dùng codemod chính thức (`@next/codemod upgrade latest`, `next-async-request-api`, `middleware-to-proxy`, `next-lint-to-eslint-cli`) thay vì sửa tay — 7 file cần async `params`/`searchParams` (customers/equipment detail+edit+list pages, route Zalo message), `src/middleware.ts` → `src/proxy.ts` (hàm `middleware()` → `proxy()`, bắt buộc ở Next 16), `.eslintrc.json` → `eslint.config.mjs` (flat config, `next lint` đã bị xóa). Mọi dependency liên quan (Radix UI, lucide-react, react-hook-form, @supabase/ssr) đã xác nhận tương thích React 19 trước khi nâng (kiểm qua `npm view <pkg> peerDependencies`). Build sạch ngay từ lần đầu (Turbopack, mặc định từ Next 16), CSP verify lại y hệt trên production build (`script-src 'unsafe-inline'` vẫn hoạt động đúng, không có violation mới), **43/43 Playwright test pass**, `npm audit` **0 lỗ hổng** (trước đó 5 HIGH), đã tự tay smoke-test qua browser (đăng nhập, xem/sửa khách hàng, xem/sửa thiết bị, tìm kiếm, dashboard, dialog Zalo) — không chỉ tin Playwright. Không phát sinh vấn đề cần quyết định thêm; đã merge master.
- **[PROMPT-15] Rà soát bảo mật OWASP (21 rule Phần 1)**: CSP + security headers (`next.config.mjs` + `src/lib/errors.ts` chuẩn hoá message lỗi không lộ chi tiết Postgres/OpenAI), rate limit nhẹ in-memory cho `/api/search` (60/phút) và `/api/customers/[id]/draft-zalo-message` (10/5 phút — route gọi OpenAI có phí thật, xem `src/lib/rate-limit.ts`), xóa hẳn trang debug `/test-connection`. CSP `script-src` dùng `'unsafe-inline'` (không phải nonce-based) — đã thử nonce nhưng xung đột với static-cache của Next.js cho các trang tĩnh (`/login`, `/`...), xem chú thích chi tiết trong `next.config.mjs`. Dev mode (`next dev`) cần thêm `'unsafe-eval'` riêng cho Fast Refresh/HMR, production thì không.
- **[PROMPT-15] Bộ test phân quyền hợp nhất**: `e2e/permissions.spec.ts` (14 test, thay vì rải rác theo từng tính năng) — test cả tầng UI lẫn gọi thẳng Supabase bằng JWT thật của từng role (`getRoleClient()` trong `e2e/helpers/auth.ts`) để xác nhận RLS chặn thật ở Postgres, không chỉ UI ẩn nút. Bao gồm 1 role `accountant` tạo tạm thời để xác nhận role chưa có tính năng vẫn an toàn mặc định (không có quyền ghi ở đâu).
- **[PROMPT-15] RULE-04 và RULE-11 (đọc lại trực tiếp từ file gốc, đối chiếu codebase)**: RULE-11 (không dùng CSS ẩn dữ liệu nhạy cảm) — ✅ Đạt, rà toàn bộ 6 chỗ dùng `<RoleGate>` chỉ bọc nút hành động (không bọc dữ liệu), mọi gating đều chặn ở server (`requireRole()`) trước khi fetch hoặc dùng conditional JSX (không tạo DOM node) thay vì CSS `display:none`. RULE-04 (không gọi database trực tiếp từ frontend) — ⚠️ Chưa đạt theo đúng chữ nghĩa: `customer-form.tsx` và tương tự (Client Component) gọi thẳng `supabase.from(...).insert()` từ browser bằng anon key. Đây là kiến trúc Supabase khuyến nghị chính thức (client gọi thẳng PostgREST, RLS làm lớp chốt chặn thay vì giấu sau custom API route) — an toàn nhờ RLS (RULE-05/06/17 đã Đạt), không phải sơ suất, và là kiến trúc xuyên suốt từ PROMPT-01. Sửa đúng chữ nghĩa 100% nghĩa là viết lại toàn bộ CRUD thành custom API route — thay đổi kiến trúc nền tảng, không tự làm, chỉ ghi nhận minh bạch.
- **[PROMPT-16] Biến môi trường Vercel — ĐÃ XÁC NHẬN XONG**: đã tự kiểm tra qua Vercel Dashboard, cả 4 biến bắt buộc (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `OPENAI_API_KEY`) đều có mặt ở **Production environment**; xác nhận `E2E_TEST_LOGIN_SECRET` **KHÔNG có** trên Vercel (chỉ tồn tại `.env.local`, đã note từ PROMPT-10) — `/api/test-login` an toàn, không bị khai thác được trên production.
- **⚠️ [PROMPT-16] Supabase free tier KHÔNG có automatic backup** (xác nhận qua docs chính thức Supabase, không phải suy đoán) — daily backup chỉ có từ gói Pro trở lên (Free tier chỉ có gợi ý tự `supabase db dump` định kỳ). **Đã xử lý ở PROMPT-16b** bằng GitHub Actions tự chạy `pg_dump` hàng ngày (xem ghi chú PROMPT-16b bên dưới) — không cần nâng Supabase lên Pro chỉ vì lý do backup nữa, vẫn có thể cân nhắc sau nếu cần thêm lý do khác.
- **[PROMPT-16] Supabase Auth "Site URL" — ĐÃ SỬA XONG**: trước đó trỏ về `http://localhost:3000` (phát hiện khi tạo thử magic link test, link tự động fallback về localhost thay vì `kiem-dinh-app.vercel.app`), đăng nhập thật email+mật khẩu không bị ảnh hưởng nhưng "Quên mật khẩu"/magic-link sẽ gửi link hỏng nếu không sửa. Đã tự vào Supabase Dashboard → Authentication → URL Configuration, đổi Site URL sang `https://kiem-dinh-app.vercel.app` và thêm redirect URL tương ứng.
- **[PROMPT-16] Đã verify (không cần bạn làm gì thêm)**: build production local sạch hoàn toàn (Turbopack, 0 lỗi/warning); RLS re-confirm thực nghiệm trên cả 4 bảng + bucket Storage (anon key luôn nhận 0 dòng); **0 dữ liệu test/rác** còn sót trong DB thật (rà theo "test", "PW TEST", "PW PERM" — sạch, kể cả các user test tạm thời từ `permissions.spec.ts` đã dọn đúng); SSL/HTTPS trên `kiem-dinh-app.vercel.app` hợp lệ (TLS handshake sạch, HSTS/CSP header khớp đúng cấu hình PROMPT-15); `/api/test-login` trả đúng 404 trên production; các route bảo vệ (`/dashboard`, `/customers`...) redirect đúng về `/login` khi chưa đăng nhập.
- **⚠️ [PROMPT-16] Chưa smoke-test được luồng ĐÃ ĐĂNG NHẬP trên production thật** — không có mật khẩu thật, và không tạo được session qua magic link vì (a) Site URL bug nêu trên, (b) trình duyệt sandbox chặn điều hướng sang domain Supabase chưa được duyệt trước. **Cần bạn tự làm** (dưới 1 phút): vào https://kiem-dinh-app.vercel.app, đăng nhập bằng tài khoản thật, xác nhận dashboard/xem KH/xem thiết bị/tìm kiếm/đăng xuất hoạt động bình thường.
- **[PROMPT-16] Dependabot — 8 PR đang mở**, không có PR nào trùng với nâng cấp đã làm ở PROMPT-15b (15b chỉ đụng next/react/react-dom/eslint*, các PR dưới đây là gói khác): an toàn/rủi ro thấp (patch/minor) — `postcss` #20, `react-hook-form` #12, `lucide-react` #11, `@supabase/supabase-js` #9; **cần xem xét kỹ trước khi merge** (major version, khả năng breaking change cao) — `tailwindcss` 3→4 #19 (viết lại cấu hình gần như hoàn toàn), `typescript` 5→7 #15, `zod` 3→4 #13 (API validation đổi, app đang dùng nhiều ở form), `@types/node` 20→26 #17. Không tự merge PR nào trong PROMPT này (ngoài phạm vi rà soát).
- **[PROMPT-16b] Backup database tự động qua GitHub Actions**: `.github/workflows/db-backup.yml` — chạy `pg_dump` hàng ngày lúc 19:00 UTC (~2:00 sáng giờ VN, `schedule` cron) + `workflow_dispatch` để chạy tay khi cần; dump nén gzip, upload bằng `actions/upload-artifact` (`retention-days: 90`). Dùng secret `SUPABASE_DB_URL` (connection string trực tiếp Postgres, không phải anon/service_role key của app) — không hardcode, không commit file dump vào git. **Cần bạn tự làm 1 lần**: thêm secret `SUPABASE_DB_URL` tại GitHub repo Settings → Secrets and variables → Actions (chi tiết + cách tải artifact về + lệnh `psql` restore ở README mục 16). Không viết test Playwright (không phải tính năng UI).
- **⚠️ [PROMPT-19] RLS DELETE `inspection_history` chỉ cấp cho admin (migration 0002)** — khi inspector submit form checklist ở PROMPT-19 mà bước insert `inspection_checklist_results` lỗi giữa chừng (vd mất mạng), bản ghi `inspection_history` vừa tạo không tự xóa bù trừ được vì RLS chặn (client không có transaction thật khi gọi PostgREST qua 2 lệnh insert riêng, nên phải tự bù trừ tay bằng 1 lệnh delete — lệnh đó bị RLS chặn nếu người thao tác là inspector), để lại bản ghi mồ côi cần Admin xóa tay. Code đã kiểm tra số dòng xóa được để báo đúng tình trạng cho người dùng (không im lặng giả vờ đã dọn sạch), chỉ chưa tự khắc phục được triệt để. **Hướng sửa sau**: thêm 1 policy RLS DELETE hẹp cho inspector (chỉ xóa được bản ghi do chính mình tạo, chưa khóa `is_locked`, chưa có `inspection_checklist_results` đi kèm) — chưa làm trong phạm vi PROMPT-19, để đánh giá riêng.

---

## 📌 Ý TƯỞNG BACKLOG (từ tài liệu tham khảo ngoài, 08/2026)

> Đã rà soát 1 tài liệu "Vibe Coding Guide" bên ngoài (khung tham khảo lớn hơn nhiều so với roadmap hiện tại, không áp dụng nguyên khung vì không phù hợp quy mô công ty ~10 người). Rút ra 3 ý đáng giữ lại:

1. **Audit Log — ƯU TIÊN CAO, cần làm TRƯỚC khi chuyển sang dữ liệu KH thật.** Khác RLS (RLS chặn quyền) — đây là bảng ghi lại lịch sử "ai đổi gì, lúc nào" cho các thay đổi quan trọng, ví dụ sửa hạn kiểm định, xóa thiết bị. Lý do ưu tiên: hệ thống kiểm định có giá trị pháp lý/đối soát, cần khả năng tra cứu lại lịch sử thay đổi. Thời điểm thực hiện: sau khi thu thập góp ý từ 7-9 đồng nghiệp dùng thử (dữ liệu giả), TRƯỚC khi dọn dữ liệu giả và chuyển sang dữ liệu thật chính thức — sẽ làm thành 1 PROMPT riêng.

2. **QR Code cho thiết bị — để dành Phase 2/3**, gắn với M4 (Điều phối thiết bị đo). Ý tưởng: mỗi thiết bị có mã QR riêng, quét tại hiện trường xem nhanh thông tin (tên, mã, KH, hạn kiểm định) mà không cần gõ tìm kiếm. Chi phí thực hiện thấp (không cần API trả phí).

3. **Tra cứu quy định bằng AI (RAG trên kho tài liệu quy chuẩn/quy định nội bộ) — để xa hơn, đánh giá riêng khi có thời gian**, không gắn vào Phase 2/3 hiện tại. Đây là dự án con phức tạp riêng (cần xây kho tài liệu đã duyệt + hệ thống tìm kiếm ngữ nghĩa/embeddings), có chi phí vận hành dài hạn (tốn API mỗi câu hỏi).

---

## 📋 PROMPT LIBRARY

| ID | Tên Prompt | Trạng thái | Ghi chú |
|----|-----------|------------|---------|
| PROMPT-01 | Setup project Next.js | ✅ Xong | Scaffold Next.js 14 App Router (TS/Tailwind/ESLint); shadcn/ui viết tay (button, input, table, card, dialog, form, select, badge, toast); Supabase client/server/middleware; /login + /test-connection (trang debug, đã xóa ở PROMPT-15); gộp luôn migration Phase 1 ban đầu (xem PROMPT-02) |
| PROMPT-02 | Supabase schema | ✅ Xong | Không phải commit riêng — schema 4 bảng (profiles/customers/equipment/inspection_history) + RLS tạm thời được tạo cùng lúc trong commit PROMPT-01 (migration 0001); RLS đầy đủ theo đúng ma trận phân quyền hoàn thiện sau ở PROMPT-03 (migration 0002) |
| PROMPT-03 | Auth + phân quyền | ✅ Xong | 2 role admin/inspector, RLS đầy đủ, đã merge master |
| PROMPT-04 | Danh sách khách hàng | ✅ Xong | Route /customers (không phải /dashboard/customers), mã KH tự sinh tăng dần, RoleGate ẩn nút Thêm theo role |
| PROMPT-05 | Form thêm/sửa khách hàng | ✅ Xong | /customers/new + /customers/[id]/edit, chỉ admin (`requireRole(["admin"])` → redirect /unauthorized); `<CustomerForm>` dùng chung (react-hook-form + zod), validate SĐT VN, MST/email optional; client gọi thẳng Supabase insert/update, RLS migration 0002 làm chốt chặn; đã merge master (PR #1) |
| PROMPT-06 | Trang chi tiết khách hàng (4 tab) | ✅ Xong | /customers/[id] thay placeholder PROMPT-04; 4 tab (Thông tin chung/Thiết bị/Lịch sử KĐ/Ghi chú), dùng `.maybeSingle()` để tránh lỗi 500 khi id không tồn tại; tách `getExpiryStatus()` riêng để PROMPT-10 dùng lại; migration 0004 nới policy SELECT `profiles` (join lịch sử KĐ → profiles bị RLS chặn ngầm tên KĐV); fix thêm ngay sau đó: Tailwind `content` glob thiếu `src/lib` khiến badge màu hạn KĐ/loại KH mất màu (lỗi có từ PROMPT-04, phát hiện ở đây); đã merge master (PR #2, #3) |
| PROMPT-07 | Danh sách thiết bị (toàn hệ thống) | ✅ Xong | /equipment JOIN customers, tìm 3 trường (tên/mã/tên KH), lọc Loại+KH kết hợp; component <ExpiryIndicator> tách dùng chung với trang chi tiết KH; nút Thêm/Sửa cho cả admin+inspector (route đích placeholder); phát hiện equipment_insert_admin chỉ cho admin — cần sửa ở PROMPT-08; đã merge master (PR #5) |
| PROMPT-08 | Form thêm/sửa thiết bị | ✅ Xong | Mã TB tự sinh qua DB trigger (TB-<số KH 3 chữ số>-<số TB 3 chữ số>); status tự tính lại từ expiry_date qua trigger, không set trực tiếp từ form; migration 0006 (RLS INSERT equipment cho admin+inspector) + 0007 (trigger sinh mã + tính status); đã merge master (PR #7) |
| PROMPT-08b | Badge "Ngừng sử dụng" hiển thị đúng | ✅ Xong | Sửa <ExpiryIndicator> nhận thêm status để hiện badge xám khi inactive |
| PROMPT-08c | Chặn trùng KH (MST cứng, tên mềm) + MST bắt buộc cho Doanh nghiệp | ✅ Xong | Migration customers_tax_code_unique_idx (partial unique index); dialog cảnh báo trùng tên; validation MST bắt buộc động theo Loại KH; đã merge master |
| PROMPT-09 | Trang chi tiết thiết bị + lịch sử KĐ | ✅ Xong | 1 trang dài cuộn xuống (không tab); dialog "+ Thêm bản ghi kiểm định" (admin+inspector) upload file PDF/JPG/PNG vào bucket Storage private `inspection-files`, xem qua signed URL ngắn hạn; trigger DB đồng bộ equipment.expiry_date/last_inspection_date khi thêm lịch sử có hạn mới; migration 0009; đã merge master (PR #8) |
| PROMPT-10 | Dashboard 4 widget chính | ✅ Xong | Thay `/dashboard` placeholder; Widget 1 (Cảnh báo hạn KĐ) gộp luôn phần đáng lẽ là PROMPT-11 riêng — đếm 3 màu + top 5 hạn gần nhất; MỌI phân loại đỏ/vàng/xanh dùng `getExpiryStatus(expiry_date)`, không dùng cột `equipment.status` (status không phân biệt được đỏ/vàng); Widget 2/3 dùng `count()` Supabase; Widget 4 vẽ thanh ngang bằng Tailwind thuần (không thêm chart lib); có bộ test Playwright (`e2e/prompt-10-dashboard.spec.ts`, 10/10 pass) đăng nhập qua magic link + route nội bộ `/api/test-login` (chặn 2 lớp: NODE_ENV + secret header `E2E_TEST_LOGIN_SECRET` chỉ có trong `.env.local`, không đặt trên Vercel); đã merge master |
| PROMPT-12 | Nút soạn tin nhắn Zalo qua AI | ✅ Xong | Ở header `/customers/[id]`, chỉ hiện khi có ≥1 thiết bị đỏ/vàng (`getExpiryStatus`, loại trừ thiết bị inactive); dialog gọi `/api/customers/[id]/draft-zalo-message` → `draftZaloMessage()` (`src/lib/ai/draft-message.ts`) — điểm DUY NHẤT gọi SDK provider, dùng **OpenAI gpt-4o-mini** làm ngoại lệ tạm thời (không phải Claude API); nội dung sửa được, nút Copy, không lưu DB; Playwright mock ở tầng route API nội bộ (không mock được `api.openai.com` trực tiếp vì lệnh gọi chạy server-side trong Route Handler, `page.route()` không chặn được request đó) — `e2e/prompt-12-zalo-message.spec.ts`, 4/4 pass; đã merge master. **Cần làm thủ công**: thêm `OPENAI_API_KEY` thật vào `.env.local` + Vercel Environment Variables. |
| PROMPT-13 | Tìm kiếm toàn hệ thống | ✅ Xong | `<GlobalSearch>` trong header dùng chung (`(dashboard)/layout.tsx`), hiện ở mọi trang; debounce 300ms → `GET /api/search` chạy song song (`Promise.all`) `customers` (company_name/code/phone) + `equipment` (name/code), mỗi nhóm tối đa 5 kết quả + `count:"exact"` để biết tổng, "Xem tất cả X kết quả" tái dùng `?q=` đã có sẵn ở `/customers` và `/equipment`; mobile thu gọn thành icon kính lúp mở overlay full-width (dùng 1 `<input>`/1 state chung, chỉ đổi class responsive, không tách 2 component); đóng dropdown khi click ra ngoài/Escape/đổi route; `e2e/prompt-13-global-search.spec.ts` 7 test mới, tổng 21/21 pass; đã merge master. |
| Branding | Logo + màu thương hiệu chính thức | ✅ Xong | `public/logo.png` (wordmark, header + `/login`, `next/image`) + `src/app/icon.png` (crop vuông icon ngôi sao+"IN" làm favicon qua quy ước file metadata Next.js — wordmark ngang gốc quá dẹt để làm favicon); `globals.css` đổi 3 biến `--primary`/`--accent`/`--ring` sang bảng màu thương hiệu (#13577E làm primary — tương phản 7.8:1 với chữ trắng; #36B4E7 chỉ dùng cho ring + tint nhạt accent — tương phản riêng nó chỉ 2.38:1, không đạt WCAG AA nếu làm nền nút); không đổi bố cục/màu ngữ nghĩa; `e2e/branding-theme.spec.ts` xác nhận logo load được (không 404) + trang vẫn chạy sau khi đổi theme, tổng 24/24 pass; đã merge master. |
| PROMPT-14 | Responsive mobile toàn hệ thống | ✅ Xong | Header dưới breakpoint `sm` thu gọn thành logo + icon tìm kiếm + hamburger (`<MobileNav>`, `src/components/nav/mobile-nav.tsx`) — nav ngang + tên user/Đăng xuất chuyển vào dropdown, đóng khi đổi route/click ra ngoài/Escape; sửa thêm: 2 dialog thiếu `max-h-[90vh] overflow-y-auto` (Zalo, cảnh báo trùng tên KH) có thể tràn dọc màn hình thấp; nút icon-only (đóng dialog, trigger tìm kiếm mobile) tăng lên ~44px vùng chạm; dialog dùng `w-[calc(100%-2rem)]` thay `w-full` để có lề 2 bên trên mobile thay vì sát mép; **phát hiện + sửa 1 regression do chính PROMPT này gây ra**: thêm `relative` vào `<header>` cho hamburger khiến dropdown `<GlobalSearch>` (đang có `sm:static`) bị lạc vị trí ở desktop — sửa thành `sm:relative`; `e2e/prompt-14-responsive-mobile.spec.ts` (375×667, kiểm tra scrollWidth mọi trang + hamburger + 2 dialog tiêu biểu), tổng 29/29 pass; tăng Playwright test timeout lên 60s (test tràn ngang chạy 9 route liên tiếp, lần đầu Next dev compile route mới có thể >30s, không phải bug); đã merge master. |
| PROMPT-15 | Rà soát bảo mật + phân quyền cuối trước deploy | ✅ Xong | **Phần A** (21 rule OWASP Phần 1): tự sửa CSP+security headers (`next.config.mjs`), chuẩn hoá lỗi không lộ chi tiết Postgres/OpenAI (`src/lib/errors.ts`, áp dụng ở customer/equipment form, list page, API route Zalo), rate limit in-memory (`src/lib/rate-limit.ts`) cho `/api/search` + route Zalo (gọi OpenAI, có phí thật), thêm `.gitignore`/`dependabot.yml`; 3 quyết định đã hỏi và được xác nhận: xóa hẳn `/test-connection` (trang debug lộ thông tin), thêm rate limit nhẹ ngay (không dùng Redis), hoãn nâng cấp Next.js 14→16 (5 CVE HIGH severity, breaking change) sang 1 PROMPT riêng (xem PROMPT-15b). **Phần B**: đối chiếu policy RLS thật (dựng lại từ lịch sử migration 0001-0009 vì sandbox không truy vấn SQL trực tiếp được) với ma trận đã chốt — khớp 100%; sau đó bạn tự chạy `SELECT * FROM pg_policies` trên DB thật xác nhận thêm, phát hiện 1 điểm lệch lịch sử ở policy `profiles` (migration 0011 đồng bộ lại). **Phần C**: phát hiện + sửa 1 lỗ hổng thật — policy INSERT bucket Storage `inspection-files` chỉ check `authenticated` thay vì đúng `admin`/`inspector`, migration `0010_storage_insert_admin_inspector.sql` (đã chạy trên DB thật, xác nhận qua đối chiếu `pg_policies`); mọi RLS khác đã an toàn mặc định cho role accountant/office (whitelist rõ ràng, không có chỗ nào check kiểu "not admin"). **Phần D**: `e2e/permissions.spec.ts` (14 test hợp nhất, khác với test rải rác theo tính năng trước đó) — test cả UI lẫn gọi thẳng Supabase bằng JWT thật từng role (`getRoleClient()` mới trong `e2e/helpers/auth.ts`) để xác nhận RLS chặn thật ở Postgres; phát hiện + sửa 1 bug thật trong lúc làm: CSP thiếu `'unsafe-eval'` ở dev mode làm hỏng mọi tương tác client-side khi chạy `next dev` (Fast Refresh dùng eval, production không bị ảnh hưởng); tổng 43/43 test pass (29 cũ + 14 mới); đã merge master. Sau đó còn xác nhận thêm RULE-04/RULE-11 (đọc lại file gốc, đối chiếu codebase — xem GHI CHÚ QUAN TRỌNG). |
| PROMPT-15b | Nâng cấp Next.js 14.2.35 → 16.3.0 (vá 5 CVE HIGH) | ✅ Xong | Quy trình cẩn thận trên nhánh riêng `claude/prompt-15b-nextjs-upgrade`: đọc upgrade guide chính thức 14→15 + 15→16, kiểm tra tương thích React 19 cho mọi dependency UI trước khi nâng, dùng codemod chính thức (`@next/codemod upgrade latest`, `next-async-request-api`, `middleware-to-proxy`, `next-lint-to-eslint-cli`) thay vì sửa tay từng file. 7 file cần async `params`/`searchParams`; `middleware.ts` → `proxy.ts`; ESLint sang flat config. Build sạch ngay lần đầu (Turbopack mặc định từ Next 16), CSP verify lại đúng như PROMPT-15 (không đổi hành vi), 43/43 Playwright pass, `npm audit` 0 lỗ hổng (trước 5 HIGH), tự tay smoke-test qua browser đủ luồng chính. Không phát sinh vấn đề cần quyết định thêm; đã merge master. |
| PROMPT-16 | Rà soát cuối trước khi coi Vercel deploy là production chính thức | ✅ Xong (còn vài mục cần bạn tự xác nhận) | Build production local sạch (Turbopack, 0 lỗi); RLS re-verify thực nghiệm trên cả 4 bảng + Storage bucket; quét sạch dữ liệu test/rác trong DB thật; SSL/HTTPS + security header trên `kiem-dinh-app.vercel.app` xác nhận đúng qua `curl`; `/api/test-login` trả 404 đúng thiết kế trên production; route bảo vệ redirect đúng khi chưa đăng nhập. Phát hiện 2 việc cần bạn xử lý: Supabase free tier không có auto-backup (khác giả định ban đầu, xác nhận qua docs chính thức — **đã xử lý ở PROMPT-16b**), Supabase Auth Site URL còn trỏ localhost (ảnh hưởng tính năng quên mật khẩu sau này, chưa ảnh hưởng đăng nhập hiện tại). Không tự đăng nhập được vào production thật (không có mật khẩu, và sandbox trình duyệt chặn điều hướng sang domain Supabase chưa duyệt) — cần bạn tự smoke-test phần đã đăng nhập. Rà 8 PR Dependabot đang mở, phân loại rủi ro thấp/cần xem xét kỹ, không tự merge. Không code gì mới, chỉ rà soát/xác nhận. |
| PROMPT-16b | Backup database tự động qua GitHub Actions | ✅ Xong hoàn toàn | `.github/workflows/db-backup.yml`: `pg_dump` hàng ngày 19:00 UTC (~2h sáng giờ VN) qua `schedule` cron + `workflow_dispatch` chạy tay; nén gzip, upload `actions/upload-artifact` (`retention-days: 90`); dùng secret `SUPABASE_DB_URL`, không hardcode/không commit file dump. Sửa lỗi version mismatch (pg_dump 16 vs Postgres 17 của Supabase) bằng cách chạy job trong container `postgres:17` chính thức. Secret `SUPABASE_DB_URL` đã thêm, đã chạy thử thành công qua `workflow_dispatch`, tải artifact về xác nhận có dữ liệu thật (226KB). README mục 16: cách tải artifact + lệnh `psql` restore. Không có test Playwright (không phải UI). |
| PROMPT-18 | Data model M3 — Checklist kiểm định (pilot: Thiết bị nâng - Cầu trục) | ✅ Xong | 3 bảng mới: `equipment_checklist_templates`/`equipment_checklist_items` (cột `has_presence_flag` boolean + `value_fields` jsonb thay vì `result_type`/`value_label`/`value_unit` ở bản thiết kế đầu — đổi lại vì 1 hạng mục có thể vừa cần cờ "Có/không có" vừa cần NHIỀU ô giá trị cùng lúc, vd hạng mục "Thử tĩnh" cần cả "Tải trọng sử dụng" lẫn "Tải trọng thử") và `inspection_checklist_results` (`presence_value` + cột `"values"` jsonb — phải để trong ngoặc kép vì `VALUES` là từ khoá reserved của Postgres); cột `inspection_history.report_metadata` jsonb (lưu phần đầu/kết luận biên bản, dùng jsonb vì quy trình còn là dự thảo). RLS: templates/items giống policy `customers` (select mọi người, ghi chỉ admin), results giống policy `inspection_history` (ghi admin+inspector có check `is_locked` qua join, xóa chỉ admin). Seed 21 hạng mục (mục A/B/C, không seed mục "Kiểm tra hồ sơ kỹ thuật" vì thang kết quả khác) cho "Thiết bị nâng - Cầu trục" — nội dung lấy từ `mau-bien-ban/KTAT/DU-THAO-QTKD-III.01-Thiet-bi-nang.pdf` Phụ lục 1; sandbox không render được ảnh PDF (thiếu poppler/pdftoppm) nên bản trích xuất text đầu tiên có vài chỗ chưa chắc (has_presence_flag hạng mục 4/5), đã gửi bảng đối chiếu cho bạn tự xem ảnh PDF gốc và sửa lại đúng theo phản hồi. `EQUIPMENT_TYPE_OPTIONS` tách "Thiết bị nâng" thành 6 loại con (Cầu trục/Cần trục/Palăng/Tời/Vận thăng nâng hàng/Bàn nâng hàng). Migration `0012`/`0013` đã chạy tay trên Supabase (bạn xác nhận, count = 21). Làm trên nhánh `claude/prompt-18-m3-checklist-schema`, merge cùng lúc với PROMPT-19. |
| PROMPT-19 | Form nhập liệu kiểm định hiện trường (checklist) | ✅ Xong | Trang `/equipment/[id]/inspect` mới — thiết bị có checklist template khớp `type` thì "+ Thêm bản ghi kiểm định" điều hướng sang đây thay vì mở `AddInspectionDialog` cũ (thiết bị không có template vẫn dùng dialog cũ y hệt, không đổi gì). Form gồm phần đầu (hình thức KĐ, kiểm định viên tối đa 2, người chứng kiến tối đa 5, địa điểm — lưu `report_metadata`), checklist 3 section A/B/C dạng collapsible (đếm "x/y đã điền" mỗi section), phần kết luận (kết quả tổng tính real-time: Đạt nếu mọi hạng mục `dat`/`khong_danh_gia`, Không đạt nếu có bất kỳ `khong_dat`). Không dùng react-hook-form (checklist load động từ DB, `value_fields` đổi shape theo từng hạng mục) — quản lý bằng `useState` + hàm `validate()` thủ công; không thêm dependency Radix accordion/radio-group — tự viết radio "dạng pill" (input thật ẩn qua `sr-only` + label 44px vừa ngón tay mobile) và section collapse bằng `useState` thường. Submit: insert `inspection_history` (map kết quả tổng `dat`→`pass`/`khong_dat`→`fail` vì cột `result` này vẫn dùng enum cũ từ migration 0001, khác enum của `inspection_checklist_results`) rồi insert 21 dòng kết quả; lỗi ở bước 2 thì tự xóa lại `inspection_history` vừa tạo (bù trừ tay, không có transaction thật qua PostgREST) — **phát hiện giới hạn thật**: RLS DELETE `inspection_history` chỉ cho admin nên bù trừ này không chạy trọn vẹn được với inspector, xem ghi chú ⚠️ [PROMPT-19] ở trên. Đã tự smoke-test qua Playwright (viết test tạm, chạy xong xóa, không commit vào repo): xác nhận cả 2 nhánh có/không có template, luồng đủ 21 hạng mục submit đúng vào DB (`inspection_history.result='pass'`, `report_metadata` đúng cấu trúc, đúng 21 dòng `inspection_checklist_results`), và viewport 375px không tràn ngang. Không làm ở PROMPT này (để dành sau): xuất Word/PDF (PROMPT-20), khóa `is_locked` (PROMPT-21), AI soạn văn bản. Merge master từ nhánh `claude/prompt-18-m3-checklist-schema`. |
| PROMPT-19b | Bổ sung ảnh kiểm định bắt buộc + kiểm tra hồ sơ + ghi nhận khác | ✅ Xong | Đối chiếu bản nháp với mẫu PDF gốc (Phụ lục 1) phát hiện form PROMPT-19 thiếu 3 phần. **(1) Ảnh kiểm định bắt buộc (mục 8.5)**: bảng `inspection_photos` mới (`category` `tong_the`/`chi_tiet_khong_dat`, RLS giống hệt `inspection_checklist_results`); `PhotoUploadField` mới (multi-file, accept `image/*`, cảnh báo khi >5 ảnh) — "Ảnh tổng thể thiết bị" luôn bắt buộc tối thiểu 1 ảnh, "Ảnh chi tiết hạng mục không đạt" chỉ hiện + bắt buộc khi Kết quả tổng = Không đạt. **(2) Kiểm tra hồ sơ kỹ thuật**: chỉ hiện đúng 1 trong 3 dòng hồ sơ theo Hình thức kiểm định đã chọn ở phần đầu (không hiện cả 3 như bản giấy); "Đánh giá kết quả" tự suy ra từ Đầy đủ/Không đầy đủ, không cho chọn tay; bắt buộc "Lý do không đạt" khi Không đầy đủ; lưu `report_metadata.kiem_tra_ho_so`. **(3) Ghi nhận khác**: 1 textarea tự do không bắt buộc, lưu `report_metadata.ghi_nhan_khac`. Submit thêm bước upload ảnh lên `ATTACHMENT_BUCKET` (path `{equipment_id}/{inspection_history_id}/{category}-{uuid}.{ext}`) rồi insert batch `inspection_photos` sau bước checklist results; tách hàm `compensateDeleteInspectionHistory()` dùng chung cho cả 2 bước có thể lỗi giữa chừng (checklist results / ảnh) — cùng giới hạn RLS DELETE chỉ admin đã ghi nhận ở PROMPT-19, không code thêm gì mới. Trang chi tiết thiết bị hiển thị thumbnail qua signed URL (`InspectionPhotoThumbnails` mới, cùng cơ chế `AttachmentLink`), nhóm theo 2 category, ở cả bảng desktop lẫn card mobile. Migration `0014_inspection_photos.sql` đã chạy tay trên Supabase production. Đã tự smoke-test qua Playwright (viết test tạm, chạy xong xóa, không commit) 2 lượt: Đạt toàn bộ (1 ảnh tổng thể — tải file thật từ Storage xuống xác nhận, không chỉ tin row DB) và có hạng mục Không đạt (đủ 2 category ảnh, thumbnail hiển thị đúng nhóm; phát hiện + sửa 1 vấn đề trong lúc viết test — không phải lỗi app — bảng desktop và card mobile luôn cùng tồn tại trong DOM nên đếm ảnh phải lọc `:visible`). Không làm: chèn ảnh vào file Word xuất ra (để PROMPT-21). Merge master từ nhánh `claude/prompt-19b-photos-hoso-ghinhan`. |
| PROMPT-20 | Thông số kỹ thuật thiết bị có cấu trúc (pilot: Thiết bị nâng - Cầu trục) | ✅ Xong | Chuẩn bị dữ liệu cho PROMPT-21 (xuất biên bản Word tự động điền) — mẫu Word cần ~9 trường thông số mà `equipment.specifications` (text tự do) không đáp ứng được. Cột `equipment.spec_values` (jsonb) mới, migration `0015_equipment_spec_values.sql` (**đã chạy tay trên Supabase**, cột tồn tại, default `{}`). `src/lib/equipment/spec-fields.ts` map equipment type → field `{key,label,unit}` (theo đúng pattern `EQUIPMENT_TYPE_GROUPS`) — trước mắt chỉ định nghĩa 9 field cho "Thiết bị nâng - Cầu trục" (mã hiệu, trọng tải thiết kế/thực tế, trọng tải đầu tự do công xôn, vận tốc nâng/hạ, vận tốc xe con, vận tốc di chuyển, khẩu độ, độ cao nâng móc, công dụng); loại khác trả về mảng rỗng, vẫn dùng `specifications` tự do như cũ, không đổi gì. Không tạo field trùng cột có sẵn (số chế tạo/năm sản xuất/nhà chế tạo/thông tin khác vẫn dùng `serial_number`/`manufacture_year`/`manufacturer`/`notes`). `equipment-form.tsx`: hiện thêm khối "Thông số kỹ thuật cầu trục" render động khi `type` = Cầu trục, đổi sang loại khác thì tự xóa `spec_values` về `{}` qua `useEffect` (tránh lưu nhầm dữ liệu loại cũ dưới loại mới) — **2 vướng mắc kiểu TypeScript đáng chú ý khi làm**: field name động `spec_values.${key}` làm vỡ suy luận generic của `zodResolver` cho **toàn bộ** form (lỗi lan sang mọi `FormField` khác), phải ép kiểu rõ ràng \`as \`spec_values.${string}\`\`; và `z.record(z.string()).default({})` trong schema cũng gây lỗi y hệt do input/output type lệch nhau của `ZodDefault` — đổi sang `.optional()` + `defaultValues.spec_values = {}` ở phía react-hook-form thay thế thì hết lỗi. `equipment/[id]/page.tsx` hiển thị thêm các trường `spec_values` có giá trị, cùng cách với "Thông số kỹ thuật" tự do. Đã tự smoke-test qua Playwright 2 lượt (viết test tạm, chạy xong xóa, không commit): lượt đầu (trước khi migration chạy) xác nhận khối hiện/ẩn đúng theo loại thiết bị, `specifications` tự do không bị ảnh hưởng, và submit lỗi hiện rõ ràng qua toast (không crash) đúng OWASP RULE-20 khi cột chưa tồn tại; lượt sau (sau khi migration chạy) xác nhận full round-trip: tạo thiết bị Cầu trục với đủ 9 field → đúng dữ liệu trong `spec_values` (đối chiếu trực tiếp DB); trang chi tiết + trang sửa load lại đúng cả 9 giá trị; đổi loại sang "Nồi hơi" rồi lưu → `spec_values` về đúng `{}`, khối thông số biến mất, trang chi tiết không còn hiện field nào. Merge master từ nhánh `claude/prompt-20-equipment-spec-values`. |
| PROMPT-21 | Xuất biên bản kiểm định ra file Word tự động điền (pilot: Thiết bị nâng - Cầu trục) | ✅ Xong | Mẫu chốt copy nguyên văn (không sửa nội dung) thành `public/report-templates/thiet-bi-nang-cau-truc.docx`. Cài `docxtemplater` + `pizzip` + `docxtemplater-image-module-free`. `src/lib/reports/thiet-bi-nang-cau-truc.ts`: hàm thuần map dữ liệu DB → tag Word — checkbox `☑`/`☐` generate động theo `item_order` thật của `equipment_checklist_items` (không hardcode 21 hạng mục); phát hiện + xử lý mismatch tên key `value_fields` hạng mục 19/20/21 giữa migration 0013 (`tai_trong_su_dung`/`tai_trong_thu`) và tag mẫu Word (`tai_su_dung`/`tai_thu`) bằng 1 bảng alias nhỏ, không đụng migration đã chạy; `ly_do_khong_dat_checklist` tự gộp note các hạng mục Không đạt thành "Hạng mục N: ghi chú"; `so_dang_ky_chung_nhan` để trống có chủ đích (Hải xác nhận chưa có số thật, KĐV tự điền tay vào file Word trước khi nộp). `src/lib/reports/generate-docx.ts`: tải ảnh qua signed URL + `createImageBitmap` lấy kích thước thật, scale-down giữ tỷ lệ trong khung 420x320px, vẽ lên `OffscreenCanvas` rồi `convertToBlob({type:"image/png"})` xuất PNG thật trước khi nhúng — **sửa 1 bug thật phát hiện qua review**: `docxtemplater-image-module-free` luôn đặt tên file nhúng là `.png` bất kể định dạng gốc, ảnh JPEG (phổ biến nhất từ điện thoại) tải thẳng vào sẽ bị khai sai content-type; đã verify lại bằng ảnh JPEG thật 1600x1200 → `file word/media/*.png` báo đúng "PNG image data, 420 x 315" (kích thước khớp đúng phép tính resize), tự mở ảnh xem lại bằng mắt xác nhận không trống/hỏng. `export-report-dialog.tsx`: nút "Xuất biên bản Word" trên mỗi dòng lịch sử kiểm định, chỉ hiện cho đúng loại thiết bị có mẫu; dialog hỏi 2 field thuộc lần kiểm định sau (hình thức KĐ lần sau, lý do rút ngắn thời hạn) trước khi merge + tải `.docx` về máy. Bổ sung thêm 2 field còn thiếu so với mẫu ở `inspect-checklist-form.tsx`: "Vị trí dán tem" (`report_metadata.vi_tri_tem`, sót từ PROMPT-19) và "Thời hạn thực hiện kiến nghị" (`report_metadata.thoi_han_kien_nghi`, cạnh ô Kiến nghị). `package.json` thêm `"overrides": {"xmldom": "npm:@xmldom/xmldom@^0.8.10"}` vá lỗ hổng critical không có bản vá của `xmldom` (dependency của image module) — `npm audit` hết critical, chỉ còn 1 high (`nanoid` qua postcss/Next.js, có sẵn từ trước, không liên quan PROMPT này); chạy lại toàn bộ 43 test Playwright cũ xác nhận override không phá API. Không cần migration DB nào. Đã tự smoke-test qua Playwright nhiều lượt (viết tạm, chạy xong xóa, không commit): đọc lại `.docx` tải về bằng pizzip xác nhận 0 tag `{` còn sót, giá trị đúng vị trí (không lẫn field, phát hiện + sửa vài lần chính test tự nhầm selector), ảnh nhúng đúng số lượng. Merge master từ nhánh `claude/prompt-21-word-export`. |
| PROMPT-22 | Thông số kỹ thuật thiết bị cho "Thiết bị nâng - Cần trục" | ✅ Xong | Mở rộng `src/lib/equipment/spec-fields.ts` (đã dựng ở PROMPT-20) — thêm entry "Thiết bị nâng - Cần trục" vào `EQUIPMENT_SPEC_FIELDS` (9 field theo mẫu Word Phụ lục 2: mã hiệu, trọng tải thiết kế/thực tế (max), vận tốc nâng, vận tốc quay, vận tốc di chuyển máy trục, tầm với thiết kế/thực tế, độ cao nâng móc khi thử (max), trọng tải ở tầm với lớn nhất thiết kế/thực tế, công dụng) — tên field **khác cầu trục dù cùng khái niệm** (vd `van_toc_nang` không phải `van_toc_nang_ha`, `tam_voi` không phải `khau_do`), đúng theo mẫu, không tự ý đổi cho khớp cầu trục. `equipment-form.tsx` đã render động theo `getEquipmentSpecFields(selectedType)` từ PROMPT-20 nên chỉ cần thêm entry là form tự hiện đúng field. **Phát hiện + sửa thêm 1 bug thật khi test** (ngoài phạm vi yêu cầu gốc): tiêu đề khối field bị hardcode cứng "Thông số kỹ thuật cầu trục" từ PROMPT-20 (lúc chỉ có 1 loại) — chọn "Cần trục" vẫn hiện sai chữ "cầu trục"; thêm hàm `getSpecSectionLabel()` suy ra tên hiển thị từ chính chuỗi `type` (`"Thiết bị nâng - X"` → `"thông số kỹ thuật x"`), tự đúng cho mọi loại thêm sau này, không cần sửa lại component. Đã tự smoke-test qua Playwright (viết tạm, chạy xong xóa, không commit): tạo thiết bị Cần trục với đủ 9 field → đối chiếu DB khớp chính xác; trang chi tiết + trang sửa load lại đúng dữ liệu (kèm chụp màn hình xác nhận heading "Thông số kỹ thuật cần trục" hiện đúng); đổi sang loại khác thì `spec_values` về `{}` và khối biến mất. Không cần migration (dùng chung cột `equipment.spec_values` đã có). Merge master từ nhánh `claude/prompt-22-can-truc-spec-fields`. |
| PROMPT-23 | Xuất biên bản Word cho "Thiết bị nâng - Cần trục" + tổng quát hoá registry export | ✅ Xong | Checklist mới `equipment_checklist_templates`/`items` cho "Thiết bị nâng - Cần trục" (22 hạng mục, Phụ lục 2) — migration `0016_seed_checklist_thiet_bi_nang_can_truc.sql`, **đã chạy tay trên Supabase từ trước**, commit lần này chỉ để khớp lịch sử migration với DB thật (file từng bị sót không commit qua các PROMPT trước). Khác cầu trục ở 3 điểm: có thêm hạng mục "Đối trọng và khung đỡ đối trọng" (14), cờ "Có/không có" ở "Đường ray"/"Cáp điện, tủ điều khiển" (cầu trục không có), và mục thử tải phức tạp hơn hẳn do "tầm với" thay đổi được (6-8 trường giá trị mỗi phép thử thay vì 2 như cầu trục). Trước khi thêm loại thiết bị thứ 2 vào luồng xuất Word, **tổng quát hoá lại kiến trúc PROMPT-21** (lúc đó hardcode cứng cho 1 loại) thành registry theo `equipment.type`: `src/lib/reports/shared.ts` (mới) tách toàn bộ phần **chung** cho mọi loại thiết bị (checklist tag động, hình thức kiểm định, hồ sơ kỹ thuật, ghi nhận khác, kết quả tổng, chứng kiến, kiểm định viên, ảnh, ngày tháng...) thành `buildCommonReportData()`; `thiet-bi-nang-cau-truc.ts` rút gọn còn gọi hàm này rồi ghi đè khối "Thông số cơ bản" riêng; `thiet-bi-nang-can-truc.ts` (mới) mirror tương tự — **không cần bảng alias tên field** như cầu trục vì đã đối chiếu trực tiếp tag thật trong `word/document.xml` với key `value_fields` migration 0016: khớp thẳng 100%. `registry.ts` (mới): `REPORT_REGISTRY` map `equipment.type` → `{templateUrl, buildData}` — cả 2 hàm build dùng chung 1 chữ ký `BuildReportDataInput` nên không cần union type/any. `generate-docx.ts`: `generateThietBiNangCauTrucReport()` → `generateReport(templateUrl, ...)` nhận template làm tham số, giữ nguyên 100% logic resize/convert PNG. `export-report-dialog.tsx`/`inspection-history-section.tsx`: tra registry theo `equipment.type` thay vì so hardcode 1 chuỗi — thêm loại thiết bị mới sau này chỉ cần 1 entry registry + 1 module build nhỏ, không phải sửa 2 file này nữa. Mẫu Word chốt copy nguyên văn thành `public/report-templates/thiet-bi-nang-can-truc.docx`. Đã tự smoke-test qua Playwright (viết tạm, chạy xong xóa, không commit): 2 lượt Cần trục (Đạt toàn bộ + có hạng mục "Đường ray" Không đạt, đủ 2 nhóm ảnh) xác nhận 0 tag `{...}` còn sót (loại trừ đúng GUID OOXML tự sinh `{28A0092B-...}` không phải tag thật) và ảnh nhúng đúng định dạng PNG thật (`file word/media/*.png` ra đúng kích thước đã resize, không phải `1 x 1`, đọc lại bằng mắt qua Read tool); thêm 1 lượt hồi quy nhanh cầu trục xác nhận tổng quát hoá không phá luồng cũ. Không cần migration mới cho riêng PROMPT này. Merge master từ nhánh `claude/prompt-23-can-truc-word-export`. |
| PROMPT-25 | Thông số kỹ thuật + xuất biên bản Word cho "Thiết bị nâng - Palăng" | ✅ Xong | Loại thiết bị thứ 3 trong registry export (PROMPT-23) — gộp chung 1 PROMPT làm cả spec fields lẫn export (không tách 2 PROMPT như Cần trục PROMPT-22/23) vì registry đã tổng quát hoá xong, không còn việc kiến trúc lớn cần làm riêng. `src/lib/equipment/spec-fields.ts`: thêm entry "Thiết bị nâng - Palăng" (7 field: mã hiệu, trọng tải thiết kế/thực tế, vận tốc nâng/hạ, vận tốc di chuyển thiết bị, chiều dài đường chạy, độ cao nâng móc, công dụng) — `getSpecSectionLabel()` (PROMPT-22) tự suy đúng "palăng" từ `type`, không cần sửa `equipment-form.tsx`. `src/lib/reports/thiet-bi-nang-pa-lang.ts` (mới): mirror Cần trục, gọi `buildCommonReportData()` + ghi đè khối "Thông số cơ bản"/"Kết luận" riêng — không cần bảng alias (đối chiếu trực tiếp tag thật `word/document.xml` với key `value_fields` migration 0017: khớp thẳng 100%, kể cả 2 field `tai_trong_su_dung`/`tai_trong_thu` — khác cầu trục nơi mẫu dùng `tai_su_dung`/`tai_thu` không có "trong"). Khác Cần trục: mẫu Palăng không có khái niệm "tầm với" nên kết luận chỉ có `trong_tai_toi_da`, không có `tam_voi_toi_da`. Checklist mới `equipment_checklist_templates`/`items` (20 hạng mục, Phụ lục 3, gộp chung Palăng điện + Palăng kéo tay vì nguồn chỉ có 1 mẫu biên bản) — migration `0017_seed_checklist_thiet_bi_nang_pa_lang.sql`, **đã chạy tay trên Supabase từ trước** (chuẩn bị sẵn từ "PROMPT-24 mentor draft"), commit lần này chỉ để khớp lịch sử migration với DB thật. Khác cầu trục 2 điểm: không có hạng mục "Buồng/cabin điều khiển" (palăng điều khiển bằng hộp bấm/tay kéo, không có cabin), và "Đường ray"/"Cáp điện, tủ điều khiển" có cờ Có/không có (giống khác biệt đã ghi nhận ở Cần trục). `registry.ts`: thêm entry `"Thiết bị nâng - Palăng"` → `{templateUrl: "/report-templates/thiet-bi-nang-pa-lang.docx", buildData: buildThietBiNangPaLangReportData}` — không đụng `export-report-dialog.tsx`/`inspection-history-section.tsx`, đúng như thiết kế registry hứa hẹn ở PROMPT-23. Mẫu Word chốt copy nguyên văn thành `public/report-templates/thiet-bi-nang-pa-lang.docx`. Đã tự smoke-test qua Playwright (viết tạm, chạy xong xóa, không commit): 2 lượt Palăng (Đạt toàn bộ + có hạng mục "Kiểm tra cáp và loại bỏ" Không đạt, đủ 2 nhóm ảnh) xác nhận 0 tag `{...}` còn sót và ảnh nhúng đúng định dạng PNG thật (`file word/media/*.png` ra đúng kích thước đã resize, đọc lại bằng mắt qua Read tool); xác nhận cleanup không sót dữ liệu/storage. Không cần migration mới cho riêng PROMPT này. Merge master từ nhánh `claude/prompt-25-palang`. |
| PROMPT-27 | Thông số kỹ thuật + xuất biên bản Word cho "Thiết bị nâng - Tời" | ✅ Xong | Loại thiết bị thứ 4 trong registry export, làm giống hệt Palăng (PROMPT-25) — gộp 1 PROMPT cả spec fields lẫn export. `src/lib/equipment/spec-fields.ts`: thêm entry "Thiết bị nâng - Tời" (6 field: mã hiệu, trọng tải thiết kế/thực tế, vận tốc tời, chiều cao nâng/chiều dài kéo, góc nghiêng lớn nhất, công dụng). `src/lib/reports/thiet-bi-nang-toi.ts` (mới): mirror Palăng, gọi `buildCommonReportData()` + ghi đè khối "Thông số cơ bản"/"Kết luận" riêng — không cần bảng alias (đối chiếu trực tiếp tag thật `word/document.xml` với key `value_fields` migration 0018: khớp thẳng 100%). Giống Palăng: kết luận chỉ có `trong_tai_toi_da`, không có `tam_voi_toi_da`. Checklist mới `equipment_checklist_templates`/`items` (20 hạng mục, Phụ lục 4, gộp chung Tời điện + Tời kéo tay) — migration `0018_seed_checklist_thiet_bi_nang_toi.sql`, **cấu trúc giống hệt Palăng** (migration 0017 mirror), chỉ khác đúng 1 chữ ở hạng mục 8 ("nâng/kéo tải" thay vì "nâng tải", không ảnh hưởng tag Word) — **đã chạy tay trên Supabase từ trước** (chuẩn bị sẵn từ "PROMPT-26 mentor draft"), commit lần này chỉ để khớp lịch sử migration với DB thật. `registry.ts`: thêm entry `"Thiết bị nâng - Tời"` → `{templateUrl: "/report-templates/thiet-bi-nang-toi.docx", buildData: buildThietBiNangToiReportData}` — không đụng `export-report-dialog.tsx`/`inspection-history-section.tsx`. Mẫu Word chốt copy nguyên văn thành `public/report-templates/thiet-bi-nang-toi.docx`. Đã tự smoke-test qua Playwright (viết tạm, chạy xong xóa, không commit): 2 lượt Tời (Đạt toàn bộ + có hạng mục "Thiết bị cơ khí – Phanh" Không đạt, đủ 2 nhóm ảnh) xác nhận 0 tag `{...}` còn sót và ảnh nhúng đúng định dạng PNG thật (`file word/media/*.png` ra đúng kích thước đã resize, đọc lại bằng mắt qua Read tool); xác nhận cleanup không sót dữ liệu/storage. Không cần migration mới cho riêng PROMPT này. Merge master từ nhánh `claude/prompt-27-toi`. |
| PROMPT-29 | Thông số kỹ thuật + xuất biên bản Word cho "Thiết bị nâng - Vận thăng nâng hàng" | ✅ Xong | Loại thiết bị thứ 5 trong registry export, gộp 1 PROMPT cả spec fields lẫn export giống Palăng/Tời. **Khác Palăng/Tời, gần cấu trúc Cầu trục hơn**: checklist 22 hạng mục (không phải 20) — có mục "Buồng/cabin điều khiển" (5, giống Cầu trục), "Đường ray" (14)/"Cáp điện, tủ điều khiển" (15) KHÔNG có cờ Có/không có (giống Cầu trục, khác Palăng/Tời), thêm 1 hạng mục hoàn toàn mới chưa từng có ở loại nào khác — "Kiểm tra điện trở nối đất CHỐNG SÉT" (17, value field riêng `gt_17_dien_tro_noi_dat_chong_set`), và hạng mục thử tải thứ 3 (22) là "Thử bộ hãm an toàn" thay vì "Thử thiết bị khống chế quá tải". `src/lib/equipment/spec-fields.ts`: thêm entry "Thiết bị nâng - Vận thăng nâng hàng" (6 field, có thêm `loai_thiet_bi` — field mới chưa loại nào khác có, vd "Vận thăng lồng"). `src/lib/reports/thiet-bi-nang-van-thang.ts` (mới): mirror Palăng/Tời, gọi `buildCommonReportData()` + ghi đè khối "Thông số cơ bản"/"Kết luận" riêng — không cần bảng alias (đối chiếu trực tiếp tag thật `word/document.xml` với key `value_fields` migration 0019: khớp thẳng 100%, kể cả field mới); chỉ có `trong_tai_toi_da`, không có `tam_voi_toi_da`. Checklist mới — migration `0019_seed_checklist_thiet_bi_nang_van_thang_nang_hang.sql`, **đã chạy tay trên Supabase từ trước** (chuẩn bị sẵn từ "PROMPT-28 mentor draft"), commit lần này chỉ để khớp lịch sử migration với DB thật. `registry.ts`: thêm entry `"Thiết bị nâng - Vận thăng nâng hàng"` → `{templateUrl: "/report-templates/thiet-bi-nang-van-thang.docx", buildData: buildThietBiNangVanThangReportData}` — không đụng `export-report-dialog.tsx`/`inspection-history-section.tsx`. Mẫu Word chốt copy nguyên văn thành `public/report-templates/thiet-bi-nang-van-thang.docx`. Đã tự smoke-test qua Playwright (viết tạm, chạy xong xóa, không commit): 2 lượt Vận thăng (Đạt toàn bộ + có hạng mục "Điện trở nối đất chống sét" Không đạt — test riêng field mới, đủ 2 nhóm ảnh) xác nhận 0 tag `{...}` còn sót và ảnh nhúng đúng định dạng PNG thật (`file word/media/*.png` ra đúng kích thước đã resize, đọc lại bằng mắt qua Read tool); xác nhận cleanup không sót dữ liệu/storage. Không cần migration mới cho riêng PROMPT này. Merge master từ nhánh `claude/prompt-29-van-thang`. |
| PROMPT-31 | Thông số kỹ thuật + xuất biên bản Word cho "Thiết bị nâng - Bàn nâng hàng" | ✅ Xong | Loại thiết bị thứ 6 trong registry export, gộp 1 PROMPT cả spec fields lẫn export giống Palăng/Tời/Vận thăng — **đây là loại CUỐI CÙNG trong nhóm 6 sub-type "Thiết bị nâng"** (Cầu trục/Cần trục/Palăng/Tời/Vận thăng nâng hàng/Bàn nâng hàng), hoàn tất toàn bộ nhóm. Checklist 20 hạng mục, không có mục "Buồng/cabin điều khiển" (giống Palăng/Tời) — nhưng **mẫu cờ Có/không có khác hẳn MỌI loại thiết bị nâng trước đó**: hạng mục 7 "Dẫn động cáp/xích" và 8 "Cụm móc cẩu, puly" CÓ cờ (điểm khác biệt DUY NHẤT so với 5 loại kia — các loại khác không có cờ ở 2 hạng mục này), hạng mục 11 "Kiểm tra cáp" và 12 "Bộ phận cố định cáp" cũng có cờ (khác mọi loại trước, chỉ có value field không có cờ), hạng mục 13 "Đường ray" có cờ nhưng 14 "Cáp điện, tủ điều khiển" thì không (lệch quy tắc so với các cặp trước, đã đối chiếu kỹ 2 lần qua ảnh PDF gốc để chắc chắn không nhầm) — hạng mục thử tải thứ 3 (20) là "Thử thiết bị khống chế quá tải" giống đa số (không phải "Thử bộ hãm an toàn" như Vận thăng). `src/lib/equipment/spec-fields.ts`: thêm entry "Thiết bị nâng - Bàn nâng hàng" (6 field: mã hiệu, trọng tải thiết kế/thực tế, chiều cao nâng thiết kế/thực tế, vận tốc nâng, kích thước bàn nâng, công dụng). `src/lib/reports/thiet-bi-nang-ban-nang.ts` (mới): mirror Palăng/Tời/Vận thăng, gọi `buildCommonReportData()` + ghi đè khối "Thông số cơ bản"/"Kết luận" riêng — không cần bảng alias (đối chiếu trực tiếp tag thật `word/document.xml` với key `value_fields` migration 0020: khớp thẳng 100%, các tag `pf_5/6/7/8/11/12/13/15/16/20` khớp đúng danh sách has_presence_flag đã đối chiếu từ DB thật); chỉ có `trong_tai_toi_da`, không có `tam_voi_toi_da`. Checklist mới — migration `0020_seed_checklist_thiet_bi_nang_ban_nang_hang.sql`, **đã chạy tay trên Supabase từ trước** (chuẩn bị sẵn từ "PROMPT-30 mentor draft"), commit lần này chỉ để khớp lịch sử migration với DB thật. `registry.ts`: thêm entry `"Thiết bị nâng - Bàn nâng hàng"` → `{templateUrl: "/report-templates/thiet-bi-nang-ban-nang.docx", buildData: buildThietBiNangBanNangReportData}` — không đụng `export-report-dialog.tsx`/`inspection-history-section.tsx`. Mẫu Word chốt copy nguyên văn thành `public/report-templates/thiet-bi-nang-ban-nang.docx`. Đã tự smoke-test qua Playwright (viết tạm, chạy xong xóa, không commit): 2 lượt Bàn nâng (Đạt toàn bộ + có hạng mục "Cụm móc cẩu, puly" Không đạt — cố ý chọn hạng mục có cờ Có/không có, điểm khác biệt riêng của loại này, để xác nhận `pf_8` vẫn đúng khi kết quả Không đạt, đủ 2 nhóm ảnh) xác nhận 0 tag `{...}` còn sót và ảnh nhúng đúng định dạng PNG thật (`file word/media/*.png` ra đúng kích thước đã resize, đọc lại bằng mắt qua Read tool); xác nhận cleanup không sót dữ liệu/storage. Không cần migration mới cho riêng PROMPT này. Merge master từ nhánh `claude/prompt-31-ban-nang`. |
| PROMPT-33 | Data model + form nhập liệu cho "Bình áp lực" | ✅ Xong | Loại thiết bị đầu tiên NGOÀI nhóm "Thiết bị nâng" (nhóm KTAT) — mẫu biên bản I-V hoàn toàn khác cấu trúc A/B/C đã quen (`mau-bien-ban/KTAT/QTKD-Binh-ap-luc.pdf`, trang 14-18). Migration `0021_seed_checklist_binh_ap_luc.sql` (**đã chạy tay trên Supabase từ trước**, chuẩn bị sẵn từ "PROMPT-32 mentor draft") seed 17 hạng mục checklist (III.3 "Kiểm tra kỹ thuật bên ngoài, bên trong" 14 mục + III.5 "Thử vận hành" 3 mục) — 2 mục này dùng thẳng `equipment_checklist_items`/`checklist-item-card.tsx` có sẵn, không cần code gì thêm. Phần còn lại của mẫu (mục I thông số, III.1 kiểm tra hồ sơ 19 dòng/3 nhóm, III.2 thiết bị dụng cụ bảng động, III.3 phụ tình trạng thiết bị an toàn, III.4 thử nghiệm, III.5.1 thử van an toàn, IV kết luận riêng) không fit checklist generic — `src/lib/equipment/spec-fields.ts` thêm entry "Bình áp lực" (8 field mục I, gồm `nuoc_che_tao` không có cột `equipment` tương ứng); `src/lib/reports/shared.ts` thêm interface `ReportMetadataBinhApLuc` (export riêng, dùng lại được cho module xuất Word) + field `ReportMetadata.binh_ap_luc` — không đổi field nào đang dùng chung cho Thiết bị nâng; `binh-ap-luc-extra-form.tsx` (mới) — 1 component `forwardRef` gộp toàn bộ phần mở rộng, state quản lý bằng 1 draft object, expose `buildMetadata()` qua ref cho form cha gọi lúc submit, mọi field optional không validate; `inspect-checklist-form.tsx` thêm `isBinhApLuc` flag ẩn khối "Kiểm tra hồ sơ kỹ thuật"/"Ghi nhận khác" dùng chung (không áp dụng loại này), render `BinhApLucExtraForm` gộp 1 khối sau checklist (tránh đặc cách theo tên section trong vòng lặp checklist dùng chung với Thiết bị nâng). CHƯA làm phần xuất Word (để PROMPT-35 sau khi mẫu Word được duyệt). Đã tự smoke-test qua Playwright (viết tạm, chạy xong xóa, không commit): xác nhận đúng 17 hạng mục checklist hiện ra, điền + lưu toàn bộ form mới vào `report_metadata.binh_ap_luc` đúng cấu trúc; **hồi quy Cầu trục** xác nhận khối hồ sơ/ghi nhận khác cũ vẫn hiện đúng, `kiem_tra_ho_so` vẫn lưu đúng, luồng Thiết bị nâng không bị ảnh hưởng. Merge master từ nhánh `claude/prompt-33-binh-ap-luc`. |
| PROMPT-35 | Xuất báo cáo Word cho "Bình áp lực" | ✅ Xong | Nối tiếp PROMPT-33 — mẫu Word đã duyệt tại `mau-bien-ban/BBKD-Binh-ap-luc-v1.docx` (MD5 `4fa0698334229ece42bf365d3ede64a5`), copy nguyên văn thành `public/report-templates/binh-ap-luc.docx`. `src/lib/reports/shared.ts`: `buildCommonReportData` thêm `data[\`cb_${n}_kdanhgia\`]` vào vòng lặp checklist chung — field CHUNG cho mọi loại thiết bị (cột "Không đánh giá"), 6 loại Thiết bị nâng hiện có không dùng tag này trong mẫu Word nên không ảnh hưởng gì (chỉ thêm 1 key thừa vô hại); Bình áp lực (mục III.3, 14/17 hạng mục) là loại đầu tiên dùng tag này. `src/lib/reports/binh-ap-luc.ts` (mới): gọi `buildCommonReportData(input)` (không cần alias, 17 hạng mục không có `value_fields`) rồi ghi đè/thêm toàn bộ phần riêng mục I/III.1/III.2/III.3 phụ/III.4/III.5.1/IV — đặc biệt phải **ghi đè `cb_hoso_dat`/`cb_hoso_kdat`** từ `binh_ap_luc.ho_so_ket_qua` (đè lên giá trị mặc định do `buildCommonReportData` set từ `kiem_tra_ho_so`, luôn null ở loại này). Toàn bộ field đọc qua optional chaining (`bal?.xxx ?? ""`), an toàn khi `report_metadata.binh_ap_luc` null (kiểm định viên chưa điền phần mở rộng) — không throw, không chặn xuất báo cáo. `registry.ts`: thêm entry `"Bình áp lực"` → `{templateUrl: "/report-templates/binh-ap-luc.docx", buildData: buildBinhApLucReportData}` — không đụng `export-report-dialog.tsx`/`inspection-history-section.tsx`/registry của 6 loại Thiết bị nâng hiện có. Trước khi code đã đối chiếu toàn bộ 214 tag trong `word/document.xml` với đúng danh sách field yêu cầu — khớp thẳng 100%. Đã tự smoke-test qua Playwright (viết tạm, chạy xong xóa, không commit): 1 lượt `binh_ap_luc` đầy đủ cả 5 mục (17 checklist gồm cả `khong_danh_gia`/`khong_dat`/`dat`) xác nhận 0 tag `{...}` sót, đúng nội dung từng mục, ảnh nhúng đúng PNG thật; 1 lượt `binh_ap_luc = null` xác nhận xuất KHÔNG lỗi, 0 tag sót, mục I vẫn đúng (độc lập với `binh_ap_luc`); phát hiện mẫu Bình áp lực không có tag `ly_do_khong_dat_checklist` (khác Thiết bị nâng) — chỉ chỉnh lại assertion test cho khớp thực tế mẫu, không phải sửa code. **Hoàn tất trọn luồng Bình áp lực** (data model PROMPT-33 + xuất Word PROMPT-35). Merge master từ nhánh `claude/prompt-35-binh-ap-luc-export`. |
| PROMPT-36 | Sao chép thiết bị (nhân bản) | ✅ Xong | Tính năng tiện ích ngoài luồng M3 checklist/xuất Word — hỗ trợ Hải nhập nhanh nhiều thiết bị cùng lô (vd 10 bình áp lực cùng thông số, chỉ khác vài chỗ). `equipment/new/page.tsx`: đọc query param `duplicateFrom`, nếu có thì fetch thiết bị nguồn (RLS select có sẵn cho mọi user đã đăng nhập, không thêm policy mới) và điền sẵn `EquipmentForm mode="create"` từ dữ liệu đó (`EquipmentForm` đã đọc `defaultValues` từ prop `equipment` sẵn dù `mode="create"`, không cần sửa component này) — bỏ qua `id`/`code` (thiết bị mới tự sinh mã riêng qua trigger, 2 field này cũng không được `EquipmentForm` đọc ở `mode="create"` nên để rỗng vô hại), **`last_inspection_date`/`expiry_date` LUÔN null và `status` LUÔN "valid"** dù thiết bị nguồn có giá trị/trạng thái gì (thiết bị mới chưa từng kiểm định, không kế thừa inactive/expired). Tiêu đề đổi thành "Thêm thiết bị (nhân bản từ `<code>` — `<name>`)" khi có nguồn; `duplicateFrom` trỏ tới id không tồn tại (đã xóa/sai) thì bỏ qua lặng lẽ, hiện form trống bình thường, không lỗi 404/500. `equipment/[id]/page.tsx`: thêm nút "Nhân bản" cạnh nút "Sửa", cùng điều kiện quyền `canEdit` (chỉ admin/inspector thấy), trỏ `/equipment/new?duplicateFrom=<id>`. Không đổi logic submit/validate của `EquipmentForm`, không đổi hành vi `mode="edit"`, không thêm cột/bảng DB — toàn bộ chỉ đọc dữ liệu có sẵn phía client/server component. Đã tự smoke-test qua Playwright (viết tạm, chạy xong xóa, không commit): nhân bản 1 thiết bị Cầu trục đủ field (kể cả 9 field `spec_values`, ngày kiểm định/hạn ở quá khứ, `status="inactive"`) xác nhận điền sẵn đúng toàn bộ trừ đúng 3 field bị loại trừ (ngày kiểm định gần nhất/hạn kiểm định để trống, checkbox "Ngừng sử dụng" không tick dù nguồn inactive) — sửa 1 field rồi lưu, đối chiếu DB xác nhận tạo thiết bị MỚI (id/mã khác hẳn), thiết bị nguồn không bị đổi gì; `/equipment/new` không có `duplicateFrom` vẫn hoạt động như cũ (form trống); `duplicateFrom` trỏ id không tồn tại không lỗi. Trong lúc viết test phát hiện 2 vướng mắc ở chính dữ liệu test tự seed (không phải lỗi code sản phẩm): thiếu đủ field `spec_values` bắt buộc theo validate có sẵn, và mã khách hàng test không khớp định dạng `KH-\d{4}-\d+` mà trigger sinh mã thiết bị (migration 0007) yêu cầu — đã sửa lại seed cho đúng. Merge master từ nhánh `claude/prompt-36-duplicate-equipment`. |
| PROMPT-37 | Import Excel thiết bị hàng loạt | ✅ Xong | Tiếp nối PROMPT-36 (nhân bản 1 thiết bị) — nhập NHIỀU thiết bị cùng lúc từ Excel, áp dụng cho MỌI loại thiết bị. Phạm vi v1 chủ động thu hẹp: chỉ field cơ bản (không có `spec_values` riêng theo loại — bổ sung sau bằng "Sửa" từng thiết bị), toàn bộ 1 lần import thuộc cùng 1 khách hàng, không sửa lỗi ngay trong bảng preview (sửa lại file gốc rồi tải lên lại). **Quyết định giữa chừng**: yêu cầu gốc dùng `xlsx` (SheetJS) nhưng bản mới nhất trên npm registry công khai (0.18.5) có 2 lỗ hổng HIGH chưa vá (Prototype Pollution CVSS 7.8, ReDoS CVSS 7.5 — SheetJS chỉ phát bản vá qua CDN riêng, không đẩy npm nữa) — đã hỏi và Hải chọn dùng `exceljs` thay thế; `exceljs` lại kéo theo `uuid` cũ có lỗ hổng moderate, vá bằng `"uuid": "^11.1.1"` trong `overrides` (giống cách vá `xmldom` ở PROMPT-21) — `npm audit` sạch, chỉ còn `nanoid` (pre-existing, không liên quan). `equipment/import/page.tsx` (mới): `requireRole(["admin","inspector"])`, fetch `customerOptions` server-side giống `new/page.tsx`. `equipment/import/import-equipment-form.tsx` (mới): luồng 3 bước — (1) chọn khách hàng, (2) tải file mẫu .xlsx (2 sheet: dữ liệu + danh sách `EQUIPMENT_TYPE_OPTIONS` hợp lệ để copy đúng chính tả) + chọn file đã điền, (3) preview validate từng dòng đọc theo TÊN CỘT header (không theo vị trí, lỡ đổi thứ tự cột vẫn đọc đúng) — tên thiết bị bắt buộc, loại thiết bị phải khớp chính xác, năm sản xuất 1900-hiện tại, chu kỳ kiểm định số nguyên dương (mặc định 12 nếu trống), hạn kiểm định chấp nhận cả `Date` object (cell định dạng ngày trong Excel) lẫn text `dd/mm/yyyy`, để trống thì null — **khác `equipmentFormSchema.expiry_date` đang bắt buộc, import tự validate riêng đơn giản hơn, không dùng lại schema đó**. Nút nhập chỉ bật khi 0 dòng lỗi toàn file (không cho nhập 1 phần). Submit 1 lệnh `insert([...])` multi-row duy nhất — để trigger sinh mã `equipment_code` (migration 0007) tính đúng thứ tự liên tục, và Postgres tự rollback toàn bộ nếu lỗi (không cần code xử lý "thành công 1 phần"). `equipment-toolbar.tsx`: thêm nút "Import Excel" cạnh "+ Thêm thiết bị", cùng `RoleGate` admin/inspector. Không đổi `EquipmentForm`/`equipmentFormSchema`, không thêm cột/bảng DB. Đã tự smoke-test qua Playwright (viết tạm, chạy xong xóa, không commit): tải file mẫu đối chiếu đúng 9 cột + đầy đủ danh sách loại thiết bị; file hợp lệ 3 dòng (có ô để trống không bắt buộc) preview 0 lỗi, nhập thành công, đối chiếu DB đúng khách hàng + mã không trùng nhau; dòng lỗi (thiếu tên/sai loại/sai năm) preview báo đúng lỗi từng dòng, khoá nút nhập; file đổi tên cột "Tên thiết bị" báo lỗi ngay từ bước đọc file, không vào preview. Merge master từ nhánh `claude/prompt-37-import-excel`. |
| PROMPT-38/39 | Data model + form + xuất Word cho "Nồi hơi" | ✅ Xong | Loại thiết bị KTAT thứ 2 ngoài nhóm Thiết bị nâng (sau Bình áp lực) — mẫu biên bản riêng, mục III phức tạp (van an toàn, đồng hồ đo mức, thử bền/vận hành). `noi-hoi-extra-form.tsx` + `ReportMetadataNoiHoi` (`lib/reports/shared.ts`) + `lib/reports/noi-hoi.ts`, entry registry `"Nồi hơi"`. Mẫu Word chốt `public/report-templates/noi-hoi.docx` (nguồn `mau-bien-ban/BBKD-Noi-hoi-v1.docx`). PROMPT-39 bổ sung riêng phần report_metadata còn thiếu so với mẫu PDF gốc sau khi đối chiếu kỹ. Trọn luồng data model+form+export xong. Merge master. |
| PROMPT-41 | Data model + form + xuất Word cho "Nồi gia nhiệt dầu" | ✅ Xong | Tương tự Nồi hơi (PROMPT-38/39) — `noi-gia-nhiet-dau-extra-form.tsx` + `ReportMetadataNoiGiaNhietDau` + `lib/reports/noi-gia-nhiet-dau.ts`, mẫu Word `public/report-templates/noi-gia-nhiet-dau.docx`. PROMPT-42 = commit/merge riêng. Hoàn tất trọn luồng. |
| PROMPT-43/44 | Quản lý dụng cụ đo (M4) — danh mục + mượn/trả | ✅ Xong | Bảng `inspection_tools` (mã tự sinh `DC-NNN`) + `inspection_tool_loans` — unique index 1 phần "1 dụng cụ chỉ có tối đa 1 lượt mượn đang mở" (`where returned_at is null`) đúng yêu cầu gốc "không trùng lặp". Trang `/tools` (danh sách + card + form + chi tiết + dialog mượn/trả + lịch sử mượn), widget cảnh báo hạn hiệu chuẩn trên dashboard. Không tích hợp GlobalSearch (quyết định chủ động thu hẹp phạm vi v1). Migration 0024. Merge master (`7caa49b`→`b49913d`). |
| PROMPT-45/46/47 | Giấy chứng nhận hiệu chuẩn cho dụng cụ đo | ✅ Xong | Bảng `inspection_tool_calibrations` (lịch sử ĐẦY ĐỦ mọi lần hiệu chuẩn, không chỉ lần mới nhất — mirror kiến trúc `inspection_history`/`equipment.expiry_date`) + trigger đồng bộ `inspection_tools.calibration_due_date`/`calibration_cert_no`. Upload file chứng nhận vào bucket `inspection-files` có sẵn (path `tool-certs/<tool_id>/<uuid>.ext`). **Bulk-import 33 giấy chứng nhận thật** (Hải cung cấp 34 file PDF scan, 1 trùng): sandbox không có OCR tiếng Việt (tesseract chỉ có gói English, không cài được `tesseract-ocr-vie` do không có sudo/mạng) nên trích dữ liệu bằng cách chuyển PDF→JPEG (`pdftoppm`) rồi đọc trực tiếp bằng mắt (Read tool vision) — an toàn hơn tin OCR không đáng tin cậy; khớp file theo SỐ CHẾ TẠO (không theo tên file) theo đúng yêu cầu Hải, phát hiện 4 lệch số chế tạo (đã hỏi và Hải đồng ý theo đề xuất), 5 áp kế không đủ thông tin phân biệt (theo thứ tự), 3 dụng cụ NDT/bê tông hoàn toàn chưa có trong danh mục gốc 48 dòng (bổ sung migration 0026). Script `scripts/bulk-upload-calibration-certs.js` (idempotent, `@supabase/supabase-js` + service-role key + Node `--env-file`) — **Claude Code Desktop tự chạy trên máy Hải**, mentor KHÔNG tự ý ghi production DB dù có kỹ thuật để làm (tiền lệ bảo mật quan trọng, đã giữ xuyên suốt). Migration 0025/0026. Merge master (`d2b2ecc`→`f087d00`). Sau đó phát hiện+sửa bug `/tools/[id]` 404 mọi dòng (PROMPT-48, không phải PROMPT riêng có số) — nguyên nhân: cache build `.next` cũ chưa nhận route mới sau merge, `rm -rf .next` + `npm run dev` lại là đủ, không phải lỗi code. |
| PROMPT-49/50/51/52 | Khóa biên bản sau khi xuất + luồng Xin sửa/Duyệt | ✅ Xong | Yêu cầu Hải: "admin và kiểm định viên đều được sửa, nhưng kiểm định viên khi muốn sửa phải được admin duyệt, khóa cho lần đó thôi". Migration 0027: RPC `lock_inspection_history()` (**bắt buộc security definer** — RLS with-check hiện có cho inspector đòi hỏi `is_locked=false` trên DÒNG SAU KHI SỬA, nên inspector không thể tự set `is_locked=true` qua update thường, phải bypass RLS qua RPC) gọi sau khi xuất Word thành công (`export-report-dialog.tsx`); bảng `inspection_edit_requests` (lý do, status pending/approved/rejected, unique 1 phần "1 yêu cầu pending/bản ghi") + trigger tự mở khóa khi duyệt. **PROMPT-50 phát hiện hệ thống chưa từng có tính năng Sửa bản ghi kiểm định nào (chỉ có Thêm mới)** — phải xây từ đầu cho cả 2 luồng (có checklist template: route `/inspect/[historyId]/edit` tái dùng `InspectChecklistForm` qua prop `mode`; không có template: `AddInspectionDialog` thêm prop `mode`/`initialData`) — quyết định đúng đắn: KHÔNG tự xóa bản ghi gốc khi lỗi ở chế độ sửa (chỉ bù trừ xóa khi tạo mới, tránh mất dữ liệu lịch sử thật). **PROMPT-51**: dialog "Xin sửa" (inspector, chặn gửi trùng + bắt lỗi race 23505) + dialog Duyệt/Từ chối (admin) + widget dashboard "Yêu cầu xin sửa đang chờ duyệt". PROMPT-52 = commit/merge (`33ece5e`→`977f5e6`, 19 file). |
| PROMPT-53/54/55 | Audit Log (equipment/customers/inspection_history) | ✅ Xong | Backlog đánh dấu ưu tiên cao — cần xong TRƯỚC khi chuyển dữ liệu giả sang dữ liệu khách hàng thật. Migration 0028: bảng `audit_log` dùng chung + trigger tổng quát `log_audit_event()` (security definer, dùng `to_jsonb(OLD)`/`to_jsonb(NEW)`) gắn vào 3 bảng cốt lõi (không mở rộng profiles/inspection_tools trong phạm vi này). Trang `/audit-log` (PROMPT-54): filter bảng/hành động/người/ngày, xem diff chi tiết (chỉ hiện field thay đổi khi update), chỉ Admin truy cập (`requireRole` + ẩn link nav). **Bug thật phát hiện sau khi build xong UI**: `old_data`/`new_data` luôn null bất kể action — nguyên nhân do migration 0028 so sánh `TG_OP` (biến đặc biệt Postgres, luôn trả về CHỮ HOA `'INSERT'/'UPDATE'/'DELETE'`) với chuỗi viết THƯỜNG trong điều kiện CASE, không bao giờ khớp; cột `action` không bị ảnh hưởng vì có gọi `lower(TG_OP)` riêng — đây là lý do action/changed_by đúng nhưng old_data/new_data sai. Xác nhận nguyên nhân bằng cách đối chiếu `select prosrc from pg_proc` thật (Hải tự chạy) với file migration — khớp 100%, chứng minh không phải lỗi deploy mà lỗi code thật. Vá bằng migration 0029 (`CREATE OR REPLACE`, giữ nguyên OID, không cần đụng trigger). PROMPT-55 = commit/merge (`3d2e756`→`331f63b`, 12 file). |
| PROMPT-56/57/58 | Module Hợp đồng — M2 v1 | ✅ Xong | Hải quyết định kéo M2 (vốn dự kiến Phase 3) lên làm ngay vì phần mềm cần có hợp đồng mới đủ dùng thực tế. Quyết định phạm vi (hỏi qua nhiều vòng AskUserQuestion): KHÔNG theo dõi báo giá riêng trong DB (vẫn dùng skill Cowork `baogia-incosaf` như hiện tại); CÓ theo dõi nhiều đợt thanh toán riêng lẻ (không chỉ 1 trạng thái chung); 1 hợp đồng gắn được NHIỀU thiết bị có sẵn trong `/equipment`; Admin+Inspector đều thêm/sửa được, chỉ Admin xóa. Migration 0030: bảng `contracts` (2 mã tách biệt — `code` tự sinh `HD-<năm>-NNN` mirror `generate_customer_code()`, và `contract_no` là số hợp đồng THẬT tự nhập tay, không tự sinh), `contract_equipment` (bảng nối), `contract_payments` (mỗi dòng 1 đợt ĐÃ thu thực tế, KHÔNG phải kế hoạch) — `contracts.paid_total` cache tự động qua trigger xử lý CẢ insert/update/delete (khác các sync trigger trước chỉ xử lý insert, vì thanh toán có thể bị sửa/xóa nhầm). Audit Log mở rộng sang 2 bảng tài chính này luôn (tái dùng `log_audit_event()`, gần như miễn phí). UI: `/contracts` (danh sách/lọc/tạo/sửa), chi tiết (dialog tìm-kiếm-nhiều-lựa-chọn để gắn thiết bị, dialog ghi nhận thanh toán — công nợ hiển thị tự tính từ trigger, KHÔNG tự tính ở client), nav link mọi role, widget "Hợp đồng còn công nợ". KHÔNG có nút xóa hợp đồng trên UI (giống quy ước customers/equipment). PROMPT-58 = vá nốt nhãn tiếng Việt cho audit-log (`contracts`/`contract_payments` bị bỏ sót ở PROMPT-54 vì lúc đó module Hợp đồng chưa tồn tại) rồi commit/merge (`be038c5`→`5bda5b0`, 28 file). **Câu hỏi treo về "chi tiết hợp đồng" đã được Hải xác nhận** (21/08/2026) — xem PROMPT-59. |
| PROMPT-59 | Số lượng/đơn giá/số tem/ngày kiểm định từng thiết bị trong hợp đồng + xuất bảng kê thiết bị | ✅ Xong, đã merge master | **Đính chính (phát hiện khi rà soát PROMPT-61, 21/08/2026)**: bản triển khai THẬT chỉ thêm 4 cột (`unit_price`/`quantity`/`so_tem`/`ngay_kiem_dinh`) — cột `unit` (đơn vị tính) dù có trong thiết kế cuối cùng của PROMPT-59.md KHÔNG được thêm thật vào migration đã chạy; bổ sung bù lại ở PROMPT-61 (migration 0033). `contract_equipment` — migration `0031_contract_equipment_details.sql` đã Hải tự chạy tay trên Supabase, xác nhận xong; `contracts.total_value` tự tính `SUM(quantity × unit_price)` qua trigger mirror `sync_contract_paid_total()`; bỏ ô "Giá trị hợp đồng" khỏi form; sửa qua dialog `edit-contract-equipment-dialog.tsx` (không inline-edit); cột Mã hiệu qua `getEquipmentModelCode()` mới (đọc `spec_values.ma_hieu ?? spec_values.loai_ma_hieu`) + Số chế tạo (`serial_number`); RLS UPDATE mới cho `contract_equipment`. Nút "Xuất bảng kê thiết bị" (`export-equipment-list-button.tsx` + `contract-equipment-list.ts`) dựng `.docx` bằng thư viện `docx` (dependency mới), không cần mẫu — đúng quyết định đã chốt: KHÔNG xuất toàn văn hợp đồng pháp lý (vẫn dùng skill `soan-hop-dong-kiem-dinh`). Claude Code tự smoke-test qua Playwright (viết→chạy→xóa, không commit rác) + `tsc --noEmit` + `npm run build` đều sạch: form hết ô nhập tay, cột Mã hiệu đúng cả 2 kiểu key lẫn case rỗng, `total_value`/dòng "Tổng cộng" khớp qua nhiều lượt sửa/gỡ, RLS inspector hoạt động, file `.docx` xuất đúng nội dung (bóc XML xác nhận). **Lưu ý dữ liệu thật**: hợp đồng `HD-2026-003` có `total_value` lệch (150.000.000đ, giá cũ nhập tay) vì chưa có dòng `contract_equipment` nào bị sửa sau migrate — đúng như đã chốt (không backfill), tự đúng khi ai đó sửa lại dòng thiết bị đó. **Đã commit + merge master**: nhánh `claude/prompt-59-chi-tiet-thiet-bi`, commit `f87a7d4` (15 file, không dính file thừa), merge `--no-ff` không conflict, push `5bda5b0..9fb652d master`, `tsc`/`build` sạch trước và sau merge, `git status` sạch. Merge commit trên master: `9fb652d`. |
| PROMPT-60 | Module Báo giá (mới) + liên kết "Tạo hợp đồng từ báo giá" | ✅ Xong, đã merge master | Đảo lại quyết định cũ (PROMPT-56: báo giá KHÔNG track trong DB) — Hải giờ muốn app tự có module Báo giá lưu trạng thái đầy đủ (nháp/đã gửi/đã chấp nhận/từ chối/hết hạn), hỗ trợ CẢ khách chưa có trong `/customers` (báo giá thường làm trước khi khách chốt) qua 5 trường snapshot trên `quotes` (không bắt buộc `customer_id`). Bảng `quotes` (mã tự sinh `BG-<năm>-NNN` mirror `generate_contract_code()`) + `quote_items` (hạng mục tự do, `item_name`/`unit` không bắt buộc gắn `equipment_id` có sẵn — khác hẳn `contract_equipment` vì báo giá xảy ra trước khi chắc chắn có thiết bị trong hệ thống). `total_value` cache tự tính mirror pattern quen thuộc. RLS/audit log mirror `contracts`. Nút "Tạo hợp đồng từ báo giá" mirror pattern "Nhân bản thiết bị" (PROMPT-36, query param `fromQuote`) — chỉ bật khi `quotes.customer_id` đã có (khách tiềm năng phải được gán customer thật trước); tự bulk-insert `contract_equipment` từ các `quote_items` CÓ `equipment_id`, item không có thì báo rõ số lượng bị bỏ qua qua toast, không im lặng; lưu `quotes.converted_contract_id` để liên kết ngược. Xuất báo giá bằng thư viện `docx` (như PROMPT-59), không cần mẫu tải lên, lưu Storage + `quote_file_path`. **Quyết định có chủ đích: KHÔNG kiểm tra đơn giá tối thiểu Thông tư 41 ở v1** — bảng giá tối thiểu phụ thuộc loại/công suất/tải trọng thiết bị, phức tạp, dữ liệu tham chiếu chưa chắc sạch kể cả trong skill `baogia-incosaf` gốc (ghi "nếu có") — để backlog, không tự chế bảng giá liều lĩnh. **Migration `0032_quotes.sql`** đã Hải tự chạy tay trên Supabase, xác nhận xong. Claude Code tự smoke-test qua Playwright (viết→chạy→xóa, không commit rác) + `tsc --noEmit` + `npm run build` đều sạch: snapshot customer đúng cho cả khách CRM lẫn khách tiềm năng; hạng mục thêm/sửa/xóa tính đúng `total_value`; nút "Tạo hợp đồng" khóa/mở đúng theo `customer_id`; luồng chuyển đổi copy đúng item có `equipment_id`, bỏ qua kèm toast rõ ràng với item không có; RLS đủ theo policy mirror `contracts`. Check DB production: 0 `quotes`, 0 `quote_items` (chưa có dữ liệu thật, đúng dự kiến). File mới: `src/app/(dashboard)/quotes/` (list/toolbar/form/pagination/attachment-link + `[id]/` detail/items-section/item-dialog/export-button/create-contract-button), `src/lib/quotes/` (status/form-schema/item-form-schema), `src/lib/reports/quote-export.ts`, `prompts/PROMPT-60-module-bao-gia.md`; sửa `contract-form.tsx`/`contracts/new/page.tsx`/`layout.tsx`. **Khoảng trống đã ghi nhận (ngoài phạm vi PROMPT-60)**: nhãn tiếng Việt cho `quotes`/`quote_items` trên `/audit-log` chưa thêm — ứng viên prompt nhỏ mirror PROMPT-57→58, tùy Hải quyết định. **ĐÃ COMMIT + MERGE MASTER** (nhánh `claude/prompt-60-module-bao-gia`, commit [c61c841](https://github.com/caotronghaiinc-prog/kiem-dinh-app/commit/c61c841), 25 file đúng phạm vi; merge `--no-ff` không conflict; push `9fb652d..9a51f1e master`; `tsc`/`build` sạch trước và sau merge; `git status` sạch). Merge commit trên `master`: `9a51f1e`. |
| PROMPT-61 | Xuất Biên bản nghiệm thu hợp đồng (tự sinh từ dữ liệu, không chỉ lưu file) | ✅ Xong (code+test), CHƯA commit/merge | Thay vì chỉ lưu file biên bản nghiệm thu đã soạn qua skill `skil-bb-nt`, app tự sinh luôn từ dữ liệu `contracts`/`contract_equipment` đã có. Văn bản pháp lý thật (căn cứ thanh toán) nên PORT NGUYÊN VĂN bố cục 16 mục đã chốt qua thực tế từ skill gốc, không tự diễn giải lại. **Rà soát trước khi chạy (mentor, 21/08/2026) phát hiện + sửa 3 điểm**: đổi số migration `0032`→`0033` (0032 đã bị PROMPT-60 dùng); phát hiện + bổ sung cột `contract_equipment.unit` (đơn vị tính) — tưởng có từ PROMPT-59 nhưng thực tế chưa (xem đính chính ở dòng PROMPT-59); làm rõ văn bản này PHẢI dùng khổ A4/dxa (9072 dxa nội dung)/Times New Roman đúng mẫu gốc, KHÔNG mirror kiểu bảng % width/font mặc định mà PROMPT-59/60 đang dùng. Thêm 8 cột mới vào `contracts` (ngày/địa điểm/kết quả nghiệm thu, đại diện Bên A + chức vụ nhập tay mỗi lần qua dialog vì không có sẵn trong `customers`, ghi chú số bản lập, file đã ký upload lại) + cột `unit` cho `contract_equipment`, cả trong 1 migration `0033_contract_acceptance.sql`. Thông tin Bên B (INCOSAF) hardcode cố định, không đọc DB. Viết mới `numberToVietnameseWords()` (`number-to-words-vi.ts`). Phạm vi v1: 1 hợp đồng chỉ nghiệm thu 1 lần trọn gói (ghi đè khi cập nhật lại), KHÔNG hỗ trợ nhiều đợt nghiệm thu từng phần. **1 sự cố quy trình**: Claude Code quên dừng lại xin Hải chạy migration trước khi test (khác quy trình đúng ở PROMPT-59/60) → debug nhầm hướng một lúc (tưởng máy chậm, hóa ra cột DB chưa tồn tại) — tự phát hiện, xin lỗi, sửa ngay; Hải xác nhận đã chạy migration (có thể 2 lần, không hỏng gì vì `add column` không tạo cột trùng — đã kiểm tra schema sạch). Test end-to-end (Playwright + render `.docx` ra ảnh xem lại bằng mắt, dọn sạch dữ liệu/file test) + `tsc --noEmit`/`npm run build` sạch: 2 nhánh "Đạt yêu cầu"/"Có vấn đề" + `signed_date` null render đúng (khổ A4 đúng, không tràn lề, cột thẳng hàng dòng "Tổng cộng" — đúng 2 lỗi thật của skill gốc, tránh được cả 2, không có dòng "Biên bản kết thúc lúc..."); `numberToVietnameseWords()` tự test 30+ giá trị (số 0 giữa, tròn chục/trăm, "mốt"/"lăm"/"linh") đối chiếu bằng mắt, đúng chuẩn; nút xuất disabled đúng khi thiếu `acceptance_date`; dialog 2 nhánh lưu đúng, "Đạt" tự null `acceptance_note`; "Đơn vị tính" nhập qua dialog sửa thiết bị lưu + hiện đúng trong file; nút file biên bản đã ký hiện đúng nhãn "Xem biên bản đã ký" (không lẫn "Xem file hợp đồng", nhờ prop `label` mới thêm vào `attachment-link.tsx`); RLS inspector cập nhật được cả nghiệm thu lẫn đơn vị tính, không bị chặn ngoài ý muốn. File mới: `acceptance-dialog.tsx`, `contract-acceptance-section.tsx`, `export-acceptance-button.tsx`, `acceptance-form-schema.ts`, `contract-acceptance.ts`, `number-to-words-vi.ts`, `prompts/PROMPT-61-...md`; sửa `types.ts`/`contracts/[id]/page.tsx`/`edit-contract-equipment-dialog.tsx`/`attachment-link.tsx`/`contract-equipment-form-schema.ts`. KHÔNG thêm cột "Đơn vị tính" vào bảng kê thiết bị PROMPT-59 (mục tùy chọn của prompt) — giữ phạm vi gọn. **Cập nhật sau PROMPT-62**: `contract-acceptance.ts` (file này) đã bị PROMPT-62 sửa thêm — cột "Đơn giá"/"Thành tiền" trong bảng nghiệm thu giờ hiển thị giá ĐÃ NHÂN VAT (khớp quy ước mới của `contract_equipment.unit_price` là "chưa VAT") để khớp đúng dòng "Tổng cộng (đã bao gồm thuế GTGT 8%)" — KHÔNG đổi cấu trúc 16 mục hay dữ liệu DB, chỉ đổi số hiển thị. **CHƯA COMMIT/MERGE** — sẽ gộp chung 1 lần commit với PROMPT-62 (xem dòng PROMPT-62), Hải sẽ báo khi sẵn sàng. |
| PROMPT-62 | Đồng bộ quy ước VAT Báo giá ⇄ Hợp đồng + làm lại file xuất Báo giá đúng mẫu thật INCERT + sửa 2 file xuất Hợp đồng liên quan | ✅ Xong (code+test), CHƯA commit/merge (gộp chung với PROMPT-61) | Bắt đầu từ phản hồi Hải: file xuất báo giá (PROMPT-60) quá đơn giản, gửi mẫu thật `Mau_Bao_Gia_INCERT.pdf` (logo + letterhead 2 cột + quốc hiệu + căn cứ TT41 + "Kính gửi"/"Địa điểm thực hiện" + bảng 3 dòng tổng Cộng chưa VAT/VAT 8%/TỔNG CỘNG + khối ký tên, header/footer lặp mỗi trang). Qua rà soát, phạm vi mở rộng: Hải yêu cầu "đơn giá giữ hợp đồng và báo giá như nhau" — nên thay vì chỉ đổi quy ước VAT ở `quote_items.unit_price` rồi nhân bù 1.08 lúc chuyển đổi sang hợp đồng, ĐỔI ĐỒNG BỘ CẢ `contract_equipment.unit_price` (PROMPT-59) sang "CHƯA gồm VAT" luôn — đã xác nhận với Hải: chưa có hợp đồng thật nào nhập đơn giá qua dialog "Sửa" (vẫn 0 mặc định) nên an toàn, không cần backfill. Migration `0034_vat_convention_and_site_location.sql`: thêm cột `quotes.site_location` (tùy chọn) + sửa CẢ 2 trigger `sync_quote_total_value()` và `sync_contract_total_value()` nhân thêm `× 1.08` (để `total_value` tiếp tục mang nghĩa "số khách phải trả", dùng tính công nợ). Luồng "Tạo hợp đồng từ báo giá" (PROMPT-60) giờ copy `unit_price` THẲNG 1:1, không nhân gì (khác phương án ban đầu đã bị bỏ). **Phát hiện qua rà soát, phải sửa thêm 2 file đã có sẵn để khỏi lệch số**: `contract-equipment-list.ts` (PROMPT-59, ĐÃ merge master) — thêm 3 dòng tách VAT như báo giá, nếu không "Tổng cộng" thiếu 8%; `contract-acceptance.ts` (PROMPT-61, CHƯA commit) — CHỈ sửa cách hiển thị cột "Đơn giá"/"Thành tiền" từng dòng (nhân 1.08 để hiển thị, KHÔNG đổi DB) cho khớp dòng "Tổng cộng (đã bao gồm thuế GTGT 8%)" đã có sẵn, KHÔNG đổi cấu trúc 16 mục đã chốt. Thêm hằng số `company-info.ts` (tên/địa chỉ/email/hotline INCERT thật — KHÁC pháp nhân INCOSAF đã hardcode ở PROMPT-61, không đụng file đó). Dùng lại `public/logo.png` có sẵn qua `ImageRun`, letterhead + header/footer dùng cơ chế `Header`/`Footer` thật của thư viện `docx` (khác 3 file export trước chỉ paste nội dung tĩnh). Bảng báo giá vẫn 1 bảng đơn (INCERT không có dịch vụ huấn luyện), căn cứ pháp lý cố định luôn là kiểm định — cả 2 điểm đã chốt qua AskUserQuestion. Giữ mã báo giá hệ thống tự sinh (`quote.code`) thay vì đổi theo khung điền tay mẫu giấy. Font Times New Roman cỡ 20 (10pt, đúng skill `baogia-incosaf` gốc, khác cỡ 26 ở Biên bản nghiệm thu — 2 loại văn bản khác nhau), khổ A4 9072 dxa nội dung + `columnSpan` cho dòng tổng (mirror lỗi kỹ thuật docx-js đã ghi nhận ở PROMPT-59/61). Lưu ý riêng: `HD-2026-003` (hợp đồng thật duy nhất) không bị ảnh hưởng thêm — vẫn lệch như ghi chú cũ ở PROMPT-59 cho tới khi ai đó sửa dòng thiết bị thật. |
| ... | ... | ... | |

---
*Template cập nhật: Sau mỗi buổi làm việc, sửa phần "Đang làm" và tick [x] những việc xong*
